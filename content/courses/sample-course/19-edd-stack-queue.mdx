---
id: edd-stack-queue
title: Estructuras de Datos · Stack y Queue
presentation: explicit
questions: none
---

{/* `none`: esta clase se publica sin banco de preguntas. Es el valor honesto */}
{/* para un banco que todavía no está escrito — se escribe como WP aparte, */}
{/* después del cierre del material, para que las preguntas midan lo que el */}
{/* documento terminó diciendo y no lo que un borrador decía. */}


# Estructuras de Datos · Stack y Queue

La clase anterior terminó con una colección que deja pedir cualquier
posición, y con el precio de esa libertad: una implementación que cumpla el
contrato entero termina con alguna operación cara. Esta clase hace lo
contrario. Estudia dos contratos que **renuncian** a esa libertad a
propósito —la **pila** y la **cola**—, los construye sobre el arreglo y
sobre la lista enlazada, y compara las dos construcciones. Al final nombra
un tercero, el **deque**, que generaliza a los dos.

Restringir no es perder. Es lo que permite prometer un costo: mientras el
único lugar donde se opera sea un extremo conocido, no hay nada que
recorrer para llegar.

<SectionBreak />

## Stack · Pila

<Slide title="Una pila">
Una **pila** es una colección con una sola puerta. Los elementos se apoyan
uno sobre otro, y el único que está a la vista es el de más arriba, al que
llamamos **cima** o **top**. Poner y sacar ocurren los dos en ese mismo
lugar.

<svg viewBox="0 0 700 320" style={{ width: '100%', maxWidth: '700px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Dos dibujos de la misma pila, cerrada abajo y abierta arriba, con 42 al fondo, 7 encima y una casilla libre en lo más alto. En el de la izquierda, rotulado push de 15, un cuadrado con el 15 baja por la abertura hacia esa casilla libre. En el de la derecha, rotulado pop, el mismo cuadrado sube y sale por la misma abertura. En los dos, una marca rotulada top señala el 7.">
  <defs>
    <marker id="pila-puerta-baja" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
    <marker id="pila-puerta-sube" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  {[
    { cx: 180, entra: true },
    { cx: 520, entra: false },
  ].map(({ cx, entra }) => (
    <g key={cx}>
      <text x={cx} y="22" textAnchor="middle" fontSize="16" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>
        {entra ? 'push(15)' : 'pop() → 15'}
      </text>

      <rect x={cx - 56} y="42" width="112" height="44" rx="3" style={{ fill: entra ? 'var(--color-accent)' : 'var(--color-surface)', stroke: 'var(--color-accent)' }} strokeWidth="1.8" strokeDasharray={entra ? undefined : '5 4'} />
      <text x={cx} y="72" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: entra ? 'var(--color-on-accent)' : 'var(--color-ink)' }}>15</text>

      <line x1={cx} y1={entra ? 96 : 136} x2={cx} y2={entra ? 134 : 98} style={{ stroke: 'var(--color-accent)' }} strokeWidth="2.2" markerEnd={entra ? 'url(#pila-puerta-baja)' : 'url(#pila-puerta-sube)'} />
      <text x={cx + 12} y={120} fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>{entra ? 'entra' : 'sale'}</text>

      <rect x={cx - 56} y="146" width="112" height="44" rx="3" style={{ fill: 'var(--color-sunk)', stroke: 'var(--color-rule)' }} strokeWidth="1.2" strokeDasharray="4 3" />
      <rect x={cx - 56} y="190" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <text x={cx} y="220" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>7</text>
      <rect x={cx - 56} y="234" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <text x={cx} y="264" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>42</text>

      <line x1={cx - 62} y1="136" x2={cx - 62} y2="284" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
      <line x1={cx + 62} y1="136" x2={cx + 62} y2="284" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
      <line x1={cx - 62} y1="284" x2={cx + 62} y2="284" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />

      <line x1={cx - 96} y1="212" x2={cx - 60} y2="212" style={{ stroke: 'var(--color-ink-faint)' }} strokeWidth="1.2" />
      <text x={cx - 100} y="216" textAnchor="end" fontSize="13" fontWeight="600" style={{ fill: 'var(--color-ink-faint)' }} fontFamily="monospace">top</text>

      <text x={cx} y="308" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>
        {entra ? 'apoya 15 sobre el top' : 'levanta el del top y lo devuelve'}
      </text>
    </g>
  ))}
</svg>

Las dos operaciones son la misma, pero en sentidos opuestos. `push`
(apilar) apoya un elemento nuevo sobre la cima; `pop` (desapilar) levanta el
de la cima y lo devuelve. No hay otra forma de acceder a los elementos: si
quieres sacar el que está en la cuarta posición desde arriba, tienes que
sacar antes los tres que tiene encima.

Los elementos salen, entonces, en el orden inverso al que entraron. Esa
regla se llama **LIFO**, del inglés _last in, first out_: **el último en
entrar es el primero en salir**.

</Slide>

<Slide title="Pilas en la vida real">
Una pila no es una idea de la programación. Es la forma de cualquier montón
donde sólo se puede tocar lo de encima.

- **Una torre de platos.** El que se apoyó último es el único que se puede
  levantar sin mover el resto.
- **El deshacer de un editor.** Cada cambio se apila cuando ocurre, y
  deshacer revierte el último, después el anterior a ése.
- **El botón «atrás» del navegador.** Cada página visitada queda sobre la
  anterior, y volver es sacar la de más arriba.
- **La pila de llamadas de un programa.** Cuando un método termina, el que
  retoma la ejecución es el que lo llamó: el último que quedó esperando. De
  ahí el nombre, y de ahí que una recursión sin caso base la desborde.

Los cuatro hacen lo mismo con palabras distintas: apoyar encima y levantar de
encima. Ese comportamiento está definido con precisión en un **tipo de dato
abstracto** (TDA) llamado **Stack**.

</Slide>


<Slide title="El TDA Stack">
El **Stack** es una colección donde sólo se trabaja en un extremo, la
**cima/top**: se pone encima y se saca de encima.

<div style={{ border: '1px solid var(--color-rule)', borderRadius: '8px', overflow: 'hidden', margin: '1.25rem 0' }}>
  <div style={{ background: 'var(--color-sunk)', padding: '0.55rem 0.9rem', borderBottom: '1px solid var(--color-rule)', fontWeight: 700 }}>Stack <span style={{ fontWeight: 400, color: 'var(--color-ink-faint)' }}>· el contrato</span></div>
  <div style={{ display: 'grid', gridTemplateColumns: 'max-content 1fr', columnGap: '2rem' }}>
  <div style={{ gridColumn: '1 / -1', padding: '0.7rem 0.9rem 0.15rem', fontSize: '0.7rem', fontWeight: 700, letterSpacing: '0.08em', color: 'var(--color-ink-faint)' }}>CREAR</div>
  <code style={{ padding: `0.15rem 0 0.7rem 0.9rem`, whiteSpace: 'nowrap' }}>build()</code>
  <span style={{ padding: `0.15rem 0.9rem 0.7rem 0` }}>construye la pila sin elementos</span>
  <div style={{ gridColumn: '1 / -1', borderTop: '1px solid var(--color-rule)', padding: '0.7rem 0.9rem 0.15rem', fontSize: '0.7rem', fontWeight: 700, letterSpacing: '0.08em', color: 'var(--color-keep)' }}>CONSULTAR · no cambian nada</div>
  <code style={{ padding: `0.15rem 0 0.3rem 0.9rem`, whiteSpace: 'nowrap' }}>top() → elemento</code>
  <span style={{ padding: `0.15rem 0.9rem 0.3rem 0` }}>el que está en la cima/top · la pila no cambia</span>
  <code style={{ padding: `0.15rem 0 0.7rem 0.9rem`, whiteSpace: 'nowrap' }}>empty() → booleano</code>
  <span style={{ padding: `0.15rem 0.9rem 0.7rem 0` }}>si no hay ningún elemento</span>
  <div style={{ gridColumn: '1 / -1', borderTop: '1px solid var(--color-rule)', padding: '0.7rem 0.9rem 0.15rem', fontSize: '0.7rem', fontWeight: 700, letterSpacing: '0.08em', color: 'var(--color-accent)' }}>MODIFICAR · cambian el estado</div>
  <code style={{ padding: `0.15rem 0 0.3rem 0.9rem`, whiteSpace: 'nowrap' }}>push(x)</code>
  <span style={{ padding: `0.15rem 0.9rem 0.3rem 0` }}>pone x en la cima/top · largo + 1</span>
  <code style={{ padding: `0.15rem 0 0.7rem 0.9rem`, whiteSpace: 'nowrap' }}>pop() → elemento</code>
  <span style={{ padding: `0.15rem 0.9rem 0.7rem 0` }}>saca el de la cima/top y lo devuelve · largo − 1</span>
  </div>
  <div style={{ padding: '0.65rem 0.9rem', borderTop: '1px solid var(--color-rule)', background: 'var(--color-sunk)' }}>
    <div style={{ fontSize: '0.7rem', fontWeight: 700, letterSpacing: '0.08em', color: 'var(--color-ink-faint)' }}>LO QUE NO DICE</div>
    <div style={{ marginTop: '0.35rem' }}>Dónde viven los elementos, si están juntos o sueltos, ni cuánto cuesta cada operación. Tampoco hay manera de pedir el segundo, ni el de más abajo: la cima/top es el único elemento visible.</div>
  </div>
</div>

Esas cuatro operaciones son todo el contrato, y **LIFO es parte del
contrato**: `pop()` no devuelve un elemento cualquiera, devuelve el último
que entró. Ninguna operación recibe una posición, así que tampoco hay forma
de pedir otro.

Para eso está `empty()`. El protocolo es: **`top()` y `pop()` sólo se pueden
llamar cuando `empty()` es falso.** Sobre una pila vacía no hay nada que
devolver, así que llamarlas ahí queda fuera del contrato.

</Slide>

<Slide title="Cómo se implementa este TDA">
El contrato no dice dónde viven los elementos, así que hay que elegir. Las
dos opciones ya las tenemos: el **arreglo**, que los guarda juntos en un
bloque de casillas, y la **lista enlazada**, que los guarda sueltos y
encadenados desde `head`.

La pila se puede construir utilizando una de las dos estructuras de datos:

| Stack | Sobre arreglo dinámico | Sobre lista simple |
|---|---|---|
| `push(x)` | escribir en `data[size]`, subir `size` (crecer si está lleno) | `insertFirst(x)` |
| `pop()` | leer `data[size - 1]`, bajar `size` | `deleteFirst()` |
| `top()` | leer `data[size - 1]` | leer el valor de `head` |
| `empty()` | `size == 0` | `head == null` |

Mira en qué punta trabaja cada una: el arreglo por el final, la lista por la
cabeza. Es la punta donde cada estructura ya era rápida, y por eso ninguna
operación tiene que buscar dónde operar.

</Slide>

<Slide title="Sobre el arreglo dinámico">
La primera implementación guarda los elementos juntos, en un bloque de
casillas consecutivas que **puede cambiar de tamaño**: cuando se llena, la
estructura reserva uno más grande y copia. El **top/cima** es el último
elemento escrito, y está siempre en `data[size - 1]`.

<svg viewBox="0 0 700 196" style={{ width: '100%', maxWidth: '700px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Un bloque de seis casillas: la capacidad reservada. Las tres primeras guardan 42, 7 y 15 —el largo—; las tres siguientes están libres y dibujadas con borde punteado. Una flecha rotulada top baja hasta la casilla de índice 2, que guarda el 15.">
  <defs>
    <marker id="stack-arr-punta" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  <text x="286" y="26" textAnchor="middle" fontSize="15" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>top</text>
  <text x="286" y="44" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>data[size − 1]</text>
  <line x1="286" y1="52" x2="286" y2="78" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#stack-arr-punta)" />
  {[0, 1, 2, 3, 4, 5].map((i) => (
    <g key={i}>
      <rect x={20 + i * 108} y="86" width="100" height="52" rx="3" style={{ fill: i < 3 ? 'var(--color-surface)' : 'var(--color-sunk)', stroke: i < 3 ? 'var(--color-rule-strong)' : 'var(--color-rule)' }} strokeWidth={i < 3 ? 1.6 : 1.2} strokeDasharray={i < 3 ? undefined : '4 3'} />
      <text x={70 + i * 108} y="120" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{['42', '7', '15', '', '', ''][i]}</text>
      <text x={70 + i * 108} y="158" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>{i}</text>
      <text x={70 + i * 108} y="180" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>{i < 3 ? '' : 'libre'}</text>
    </g>
  ))}
</svg>

Las cuatro invariantes del arreglo dinámico siguen valiendo tal cual:

1. **`0 ≤ size ≤ data.length`.** El largo nunca supera la capacidad.
2. **Los elementos son exactamente `data[0]` hasta `data[size - 1]`.** Juntos
   y desde la posición 0, sin huecos en el medio.
3. **`data[size..]` es basura.** Existe, tiene algún valor, y nadie lo lee.
4. **`data.length ≤ 4 × size`.** La capacidad no sobra: nunca hay más de
   cuatro casillas por elemento.

Y la pila agrega la suya:

5. **La cima es `data[size - 1]`.** Cuando `size` es cero no hay cima, y eso
   es exactamente una pila vacía.

La quinta es la que hace barata a esta implementación: la cima no hay que
buscarla, es una resta.

</Slide>

<Slide title="push sobre arreglo dinámico">
`push` **es `insertLast` sobre un arreglo dinámico**: agregar el elemento
que sigue en la cima y avanzar la cima. La pila no inventa una operación
nueva, le pone otro nombre a la que el arreglo ya tenía.

<SequenceStepper
  eda="dynamic-array"
  capacity={4}
  operation="insert-last"
  values={[42, 7]}
  value={[15, 4, 9, 23]}
  pointer="top"
  showCode={false}
/>

En el **peor caso** `push` cuesta $$\Theta(N)$$: cuando el bloque está
lleno hay que reservar uno del doble y copiar los $$N$$ elementos antes de
poder escribir. Cualquier otra llamada escribe en una casilla libre y sube
`size`, que es $$\Theta(1)$$.

El costo **amortizado** es $$\Theta(1)$$: como la capacidad se duplica,
entre dos llamadas caras hay $$N$$ baratas, así que el precio de la copia
repartido entre todas ellas es constante por llamada.

</Slide>

<Slide title="pop sobre arreglo dinámico">
`pop` **es `deleteLast` sobre un arreglo dinámico**: devolver el elemento de
la cima y retroceder la cima. No borra nada —el valor sigue escrito donde
estaba, y es la invariante que dice que los elementos válidos llegan hasta
`data[size - 1]` la que lo convierte en basura que nadie va a leer.

<SequenceStepper
  eda="dynamic-array"
  capacity={8}
  operation="remove-last"
  values={[42, 7, 15, 4, 9, 23]}
  times={3}
  pointer="top"
  showCode={false}
/>

En el **peor caso** `pop` cuesta $$\Theta(N)$$: al bajar `size` la capacidad
puede pasar a sobrar —`data.length > 4 × size`— y entonces el bloque se
achica, que es reservar uno más chico y copiar los elementos que quedan.
Cualquier otra llamada lee `data[size - 1]`, baja `size` y termina:
$$\Theta(1)$$.

El costo **amortizado** es $$\Theta(1)$$, por la misma razón que en `push`:
el achique ocurre sólo cuando el largo baja al cuarto de la capacidad, y al
achicar vuelve a quedar en la mitad. Hasta el próximo hay $$N$$ `pop` que no
copian nada.

</Slide>

<Slide title="Costos de Stack sobre arreglo dinámico">
| Operación | Peor caso | Amortizado | Invariante que restaura |
|---|---|---|---|
| `build()` | $$\Theta(1)$$ | $$\Theta(1)$$ | Deja `size` en cero: una pila sin cima |
| `top()` | $$\Theta(1)$$ | $$\Theta(1)$$ | Ninguna: lee y no cambia nada |
| `empty()` | $$\Theta(1)$$ | $$\Theta(1)$$ | Ninguna: `size` ya guarda el dato |
| `push(x)` | <span style={{ color: 'var(--color-mark)', fontWeight: 700 }}>$$\Theta(N)$$</span> | $$\Theta(1)$$ | La cima es `data[size - 1]`, y `size ≤ data.length` obliga a crecer |
| `pop()` | <span style={{ color: 'var(--color-mark)', fontWeight: 700 }}>$$\Theta(N)$$</span> | $$\Theta(1)$$ | La cima es `data[size - 1]`, y `data.length ≤ 4 × size` obliga a achicar |

**Lo que gana.** Las cuatro operaciones son aritmética sobre `size`: no hay
nada que recorrer ni ningún enlace que seguir. Y los valores quedan uno al
lado del otro en memoria, que es como el procesador los lee más rápido.

**Lo que paga.** `push` y `pop` casi siempre cuestan un paso, pero cada
tanto copian el arreglo entero. En promedio sale constante; una llamada
suelta puede tardar $$N$$, y no se sabe cuál va a ser.

Si lo que importa es el tiempo total de un programa, manda el promedio y
esto es una buena elección. Si lo que importa es cuánto puede tardar **una**
operación —un videojuego que dibuja 60 cuadros por segundo, un sistema de
tiempo real—, manda el peor caso, y esas copias son un problema.

La lista enlazada no las tiene: nunca copia nada.

</Slide>

<Slide title="Sobre la lista enlazada">
La segunda implementación guarda cada elemento en un nodo suelto que apunta
al siguiente, con una referencia `head` al primero de la cadena.

La decisión que la hace funcionar es una sola: **la cima es la cabeza**.

<svg viewBox="0 0 700 170" style={{ width: '100%', maxWidth: '700px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Una lista enlazada de tres nodos con los valores 15, 7 y 42. La referencia head apunta al primero y está rotulada como la cima; cada nodo apunta al siguiente y el último apunta a null.">
  <defs>
    <marker id="stack-lista-punta" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-ink-soft)' }} />
    </marker>
    <marker id="stack-lista-punta-head" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  <text x="46" y="30" textAnchor="middle" fontSize="15" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>head</text>
  <text x="46" y="50" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>= la cima</text>
  <line x1="46" y1="58" x2="46" y2="88" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#stack-lista-punta-head)" />
  {[15, 7, 42].map((v, i) => (
    <g key={i}>
      <rect x={20 + i * 190} y="92" width="86" height="54" rx="4" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <rect x={106 + i * 190} y="92" width="38" height="54" rx="4" style={{ fill: 'var(--color-sunk)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <text x={63 + i * 190} y="127" textAnchor="middle" fontSize="24" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
      <circle cx={125 + i * 190} cy="119" r="4" style={{ fill: 'var(--color-ink-soft)' }} />
      <line x1={125 + i * 190} y1="119" x2={i < 2 ? 200 + i * 190 : 620} y2="119" style={{ stroke: 'var(--color-ink-soft)' }} strokeWidth="1.6" markerEnd="url(#stack-lista-punta)" />
    </g>
  ))}
  <text x="638" y="124" fontSize="15" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>null</text>
</svg>

Con esa decisión, las dos operaciones que la pila pide caen sobre el
principio de la cadena, que es el único lugar al que `head` llega sin
caminar. Una pila vacía es `head == null`.

La estructura entera son dos atributos y sus cuatro métodos, para respetar
la implementación del TDA:

```java
class Stack {
    private Node head;  // el primer nodo de la cadena: la cima
    private int size;   // cuántos elementos hay

    void push(int x)    // pone x en la cima
    int pop()           // saca el de la cima y lo devuelve
    int top()           // el de la cima, sin sacarlo
    boolean empty()     // si la pila no tiene ningún elemento
    int size()          // cuántos elementos tiene
}
```

</Slide>

<Slide title="Las invariantes de la pila sobre lista">
Tres propiedades sostienen esta implementación, y cada operación las deja
como las encontró.

1. **`head` apunta al primer nodo, o a `null` si la pila está vacía.** Es el
   único punto de entrada a la cadena. Si `head` queda apuntando a un nodo
   del medio, todo lo anterior se vuelve inalcanzable: sigue en memoria y no
   hay manera de llegar.

2. **Desde `head`, siguiendo `next`, se llega a todos los nodos y se termina
   en `null`.** La cadena no tiene cortes ni ciclos.

3. **La cima es el nodo al que `head` apunta.** Es la traducción del
   contrato a esta estructura, y es la que decide todos los costos: la
   cima es el único nodo al que se llega sin caminar.

La tercera es la que hace barata a esta implementación, igual que en el
arreglo lo era «la cima es `data[size - 1]`». Cambia la estructura, no el
papel que juega la invariante.

Lo que sí cambia es el trabajo que hay que hacer para llegar a la cima. En
el arreglo era una resta, `size - 1`, y después ir a esa casilla. Acá no hay
nada que calcular: `head` **es** la cima, y basta con mirarla.

</Slide>

<Slide title="Las consultas">
Las tres operaciones que no cambian la pila salen de los dos atributos, sin
tocar la cadena.

```java
boolean empty() {
    return size == 0;
}

int size() {
    return size;
}

int top() {
    if (head == null) {
        throw new NoSuchElementException();
    }
    return head.value;
}
```

- **`empty()`** dice si la pila no tiene elementos. Lee un campo: $$\Theta(1)$$.
- **`size()`** devuelve cuántos hay. También un campo, y por eso es
  $$\Theta(1)$$: el largo se mantiene en cada `push` y cada `pop`, así que
  nunca hay que recorrer la cadena para contarlo.
- **`top()`** devuelve el valor de la cima sin sacarla. `head` ya apunta ahí,
  así que tampoco hay nada que recorrer: $$\Theta(1)$$.

Ésa es toda la diferencia entre `top` y `pop`: **`pop` devuelve la cima y la
saca; `top` la devuelve y deja la pila como estaba.**

</Slide>

<Slide title="Push">
Implementar `push` es **usar `insertFirst` sobre una lista enlazada simple**.
La pila no agrega trabajo: le pone su nombre a la operación que la lista ya
hacía, y le quita la posibilidad de elegir dónde.

<svg viewBox="90 82 520 192" style={{ width: '100%', maxWidth: '660px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Dos dibujos de la misma pila, cerrada abajo y abierta arriba. A la izquierda, antes: 42 al fondo, 7 encima y la casilla de la cima libre. Una flecha rotulada push de 9 lleva al de la derecha, después, donde el 9 ocupa la cima y está resaltado.">
  <defs>
    <marker id="pila-push-flecha" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  <rect x={104} y="100" width="112" height="44" rx="3" style={{ fill: 'var(--color-sunk)', stroke: 'var(--color-rule)' }} strokeWidth="1.2" strokeDasharray="4 3" />
  <rect x={104} y="144" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
  <text x={160} y="174" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>7</text>
  <rect x={104} y="188" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
  <text x={160} y="218" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>42</text>
  <line x1={98} y1="92" x2={98} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <line x1={222} y1="92" x2={222} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <line x1={98} y1="238" x2={222} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <rect x={484} y="100" width="112" height="44" rx="3" style={{ fill: 'var(--color-accent)', stroke: 'var(--color-accent)' }} strokeWidth="1.8" />
  <text x={540} y="130" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-on-accent)' }}>9</text>
  <rect x={484} y="144" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
  <text x={540} y="174" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>7</text>
  <rect x={484} y="188" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
  <text x={540} y="218" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>42</text>
  <line x1={478} y1="92" x2={478} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <line x1={602} y1="92" x2={602} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <line x1={478} y1="238" x2={602} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <text x="350" y="152" textAnchor="middle" fontSize="15" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>push(9)</text>
  <line x1="240" y1="170" x2="458" y2="170" style={{ stroke: 'var(--color-accent)' }} strokeWidth="2.2" markerEnd="url(#pila-push-flecha)" />
  <text x="160" y="262" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>antes</text>
  <text x="540" y="262" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>después</text>
</svg>

```java
void push(int x) {
    Node fresh = new Node(x);
    fresh.next = head;
    head = fresh;
    size++;
}
```

`push` toca dos cosas y nada más: el nodo nuevo y `head`. Los que ya estaban
no se mueven ni se enteran, así que la operación cuesta lo mismo con cero
elementos que con un millón: $$\Theta(1)$$.

Y acá no hay capacidad que se pueda acabar. Cada `push` pide su propio nodo
cuando lo necesita, así que la pila nunca tiene que mudarse a un bloque más
grande ni copiar lo que ya tenía.

</Slide>

<Slide title="Push">
<SequenceStepper
  eda="linked-list-singly"
  operation="insert-first"
  values={[]}
  value={[42, 7, 15]}
  method="push"
  receiver="pila"
  receiverType="Stack"
/>

</Slide>

<Slide title="Pop">
Implementar `pop` es **usar `deleteFirst` sobre la misma lista**: sacar el
primer nodo de la cadena y devolver su valor.

<svg viewBox="90 82 520 192" style={{ width: '100%', maxWidth: '660px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Dos dibujos de la misma pila, cerrada abajo y abierta arriba. A la izquierda, antes: 42 al fondo, 7 encima y el 9 en la cima. Una flecha rotulada pop devuelve 9 lleva al de la derecha, después, donde la casilla de la cima quedó libre y la cima vuelve a ser el 7.">
  <defs>
    <marker id="pila-pop-flecha" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  <rect x={104} y="100" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.8" />
  <text x={160} y="130" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>9</text>
  <rect x={104} y="144" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
  <text x={160} y="174" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>7</text>
  <rect x={104} y="188" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
  <text x={160} y="218" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>42</text>
  <line x1={98} y1="92" x2={98} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <line x1={222} y1="92" x2={222} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <line x1={98} y1="238" x2={222} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <rect x={484} y="100" width="112" height="44" rx="3" style={{ fill: 'var(--color-sunk)', stroke: 'var(--color-rule)' }} strokeWidth="1.2" strokeDasharray="4 3" />
  <rect x={484} y="144" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
  <text x={540} y="174" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>7</text>
  <rect x={484} y="188" width="112" height="44" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
  <text x={540} y="218" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>42</text>
  <line x1={478} y1="92" x2={478} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <line x1={602} y1="92" x2={602} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <line x1={478} y1="238" x2={602} y2="238" style={{ stroke: 'var(--color-rule-strong)' }} strokeWidth="2.6" strokeLinecap="round" />
  <text x="350" y="152" textAnchor="middle" fontSize="15" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>pop() → 9</text>
  <line x1="240" y1="170" x2="458" y2="170" style={{ stroke: 'var(--color-accent)' }} strokeWidth="2.2" markerEnd="url(#pila-pop-flecha)" />
  <text x="160" y="262" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>antes</text>
  <text x="540" y="262" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>después</text>
</svg>

```java
int pop() {
    if (head == null) {
        throw new NoSuchElementException();
    }
    int x = head.value;
    head = head.next;
    size--;
    return x;
}
```

Una pila vacía no tiene cima, así que `pop()` no puede devolver un valor: lo
primero que hace es preguntar y fallar.

`pop` toca dos cosas y nada más: `head` y `size`. No recorre la cadena ni
mueve a nadie, y por eso no importa si la pila tiene tres elementos o un
millón —el trabajo es el mismo—: $$\Theta(1)$$. Costaría $$\Theta(N)$$ si
hubiera que buscar el nodo que sale, y no hay que buscarlo: `head` ya lo
tiene.

El nodo que sale queda sin nadie apuntándolo, y el colector de basura lo
recupera más tarde.

</Slide>

<Slide title="Pop">
<SequenceStepper
  eda="linked-list-singly"
  operation="remove-first"
  values={[15, 7, 42]}
  times={3}
  method="pop"
  receiver="pila"
  receiverType="Stack"
/>

</Slide>

<Slide title="Costos de Stack sobre lista">
| Operación | Peor caso | Invariante que restaura |
|---|---|---|
| `build()` | $$\Theta(1)$$ | Deja `head` en `null` y `size` en cero |
| `top()` | $$\Theta(1)$$ | Ninguna: lee `head.value` y no cambia la cadena |
| `empty()` | $$\Theta(1)$$ | Ninguna: `size` ya guarda el dato |
| `size()` | $$\Theta(1)$$ | Ninguna: devuelve el campo que las otras mantienen |
| `push(x)` | $$\Theta(1)$$ | `head` apunta a la cima: el nodo nuevo enlaza al que lo era |
| `pop()` | $$\Theta(1)$$ | `head` apunta a la cima: pasa a ser el segundo |

Todo constante, y por una sola razón: cada operación trabaja sobre el primer
nodo, que es el único al que se llega sin caminar la cadena.

Y hay algo que esta tabla **no** tiene. No hay columna de amortizado, porque
no hace falta: la lista nunca copia nada, así que el peor caso de cada
operación es también su caso típico. No hay bloque que se pueda llenar —cada
nodo se pide cuando hace falta y se suelta cuando sobra—, y por eso ninguna
fila dice $$\Theta(N)$$.

Es exactamente lo que el arreglo no podía prometer.

</Slide>

<Slide title="Arreglo vs lista">
| | Sobre arreglo dinámico | Sobre lista simple |
|---|---|---|
| `push(x)` | $$\Theta(1)$$ amortizado — $$\Theta(N)$$ la corrida que copia | $$\Theta(1)$$ siempre |
| `pop()` | $$\Theta(1)$$ amortizado — $$\Theta(N)$$ la que achica | $$\Theta(1)$$ siempre |
| `top()`, `empty()` | $$\Theta(1)$$ | $$\Theta(1)$$ |
| memoria por elemento | el valor | el valor **y** un enlace |
| capacidad | crece y se achica, copiando | sin límite previo |
| los elementos en memoria | juntos | sueltos |

Las dos implementaciones cumplen el contrato con los mismos costos
constantes, así que la elección no se decide ahí. Se decide en las tres
filas de abajo.

El arreglo gana en memoria y en localidad: no paga un enlace por elemento y
guarda los valores juntos, que es lo que el procesador prefiere leer. Lo
paga en copias, baratas en promedio pero no en el instante en que ocurren.

La lista gana cuando ninguna corrida puede permitirse la pausa de una copia. Paga un enlace por elemento y
esparce los valores, y a cambio ninguna operación tiene un peor caso
distinto de su caso típico.

</Slide>

<Slide title="Stack en Java">
Java trae la pila hecha, en `java.util.Stack`, y sus nombres son casi los
del contrato:

| El TDA | Java |
|---|---|
| `push(x)` | `push(x)` |
| `pop()` | `pop()` |
| `top()` | `peek()` |
| `empty()` | `empty()` |
| `size()` | `size()` |

```java
Stack<Integer> stack = new Stack<>();
stack.push(42);
stack.push(7);

int top = stack.peek();  // 7, y la pila queda igual
int x = stack.pop();      // 7, y ahora la pila sólo tiene el 42
```

Sobre una pila vacía, `pop()` y `peek()` lanzan `EmptyStackException`: no
devuelven cero ni ninguna otra cosa. Es el contrato de la lámina del TDA
cumplido tal cual, y preguntar `empty()` antes sigue siendo trabajo de quien
usa la pila.

</Slide>

export function Paso({
  expr = '',
  sep = '',
  cursor = -1,
  pila = [],
  etiqueta = 'pila',
  nota,
  falla = false,
}) {
  const tokens = expr.split(sep).filter((t) => t !== '');
  // Sin expresión que recorrer —Josefo, la ventana móvil— la fila de arriba
  // no existe y la estructura sube a ocupar su lugar.
  const solo = tokens.length === 0;
  const yEst = solo ? 10 : 72;
  const CH = 40;
  const W = 34;
  const X = 92;
  const marco = (malo) => (malo ? 'var(--color-flag)' : 'var(--color-mark)');
  return (
    <svg
      viewBox={`0 0 560 ${solo ? 86 : 148}`}
      style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }}
      role="img"
      aria-label={`${nota} ${solo ? '' : `La expresión es ${tokens.join(' ')}. `}La ${etiqueta} contiene ${pila.length === 0 ? 'nada' : pila.join(' ')}.`}
    >
      {solo ? null : (
        <text x="8" y="32" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>
          expresión
        </text>
      )}
      {tokens.map((c, i) => (
        <g key={i}>
          <rect
            x={X + i * CH}
            y="10"
            width={W}
            height="34"
            rx="3"
            style={{
              fill: i === cursor ? (falla ? 'var(--color-flag)' : 'var(--color-accent)') : 'var(--color-surface)',
              stroke: i === cursor ? (falla ? 'var(--color-flag)' : 'var(--color-accent)') : 'var(--color-rule-strong)',
            }}
            strokeWidth="1.6"
            opacity={i < cursor ? 0.45 : 1}
          />
          <text
            x={X + i * CH + W / 2}
            y="34"
            textAnchor="middle"
            fontSize="18"
            fontWeight="600"
            fontFamily="monospace"
            opacity={i < cursor ? 0.45 : 1}
            style={{ fill: i === cursor ? 'var(--color-on-accent)' : 'var(--color-ink)' }}
          >
            {c}
          </text>
        </g>
      ))}
      <text x="8" y={yEst + 22} fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>
        {etiqueta}
      </text>
      {pila.length === 0 ? (
        <text x={X} y={yEst + 22} fontSize="14" style={{ fill: 'var(--color-ink-faint)' }}>
          vacía
        </text>
      ) : (
        pila.map((c, i) => (
          <g key={i}>
            <rect
              x={X + i * CH}
              y={yEst}
              width={W}
              height="34"
              rx="3"
              style={{ fill: 'var(--color-mark-soft)', stroke: marco(falla && i === pila.length - 1) }}
              strokeWidth="1.6"
            />
            <text
              x={X + i * CH + W / 2}
              y={yEst + 24}
              textAnchor="middle"
              fontSize="18"
              fontWeight="600"
              fontFamily="monospace"
              style={{ fill: 'var(--color-ink)' }}
            >
              {c}
            </text>
          </g>
        ))
      )}
      {pila.length > 0 ? (
        <text x={X + pila.length * CH + 6} y={yEst + 24} fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>
          {etiqueta === 'pila' ? '← cima' : '← final'}
        </text>
      ) : null}
      <text x="8" y={yEst + 64} fontSize="13" style={{ fill: falla ? 'var(--color-flag)' : 'var(--color-ink-soft)' }}>
        {nota}
      </text>
    </svg>
  );
}

<Slide title="Paréntesis balanceados">
Una expresión está **balanceada** si cada símbolo que se abre se cierra
después, y en el orden correcto. Dibujando un arco entre cada par se ve de
un golpe:

<svg viewBox="40 -8 580 340" style={{ width: '100%', maxWidth: '660px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Cuatro expresiones con un arco que une cada par de símbolos. En la primera, abre paréntesis llave corchete y sus tres cierres, los arcos se anidan sin cruzarse y está balanceada. En la segunda, abre paréntesis corchete cierra paréntesis cierra corchete, los dos arcos se cruzan. En la tercera, dos aperturas y un cierre, el primer paréntesis queda sin arco. En la cuarta, un par completo y un cierre final que no tiene con qué emparejarse.">
  <g transform="translate(0, 0)">
    <rect x={56} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={70} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'('}</text>
    <rect x={88} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={102} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'{'}</text>
    <rect x={120} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={134} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'['}</text>
    <rect x={152} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={166} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{']'}</text>
    <rect x={184} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={198} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'}'}</text>
    <rect x={216} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={230} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{')'}</text>
    <path d="M 70 31 C 70 75, 230 75, 230 31" fill="none" style={{ stroke: 'var(--color-keep)' }} strokeWidth="1.8" />
    <path d="M 102 31 C 102 61, 198 61, 198 31" fill="none" style={{ stroke: 'var(--color-keep)' }} strokeWidth="1.8" />
    <path d="M 134 31 C 134 47, 166 47, 166 31" fill="none" style={{ stroke: 'var(--color-keep)' }} strokeWidth="1.8" />
    <text x="300" y="20" fontSize="14" fontWeight="700" style={{ fill: 'var(--color-keep)' }}>balanceada · los arcos anidan</text>
  </g>
  <g transform="translate(0, 90)">
    <rect x={56} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={70} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'('}</text>
    <rect x={88} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={102} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'['}</text>
    <rect x={120} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={134} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{')'}</text>
    <rect x={152} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={166} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{']'}</text>
    <path d="M 70 31 C 70 51, 134 51, 134 31" fill="none" style={{ stroke: 'var(--color-flag)' }} strokeWidth="1.8" />
    <path d="M 102 31 C 102 69, 166 69, 166 31" fill="none" style={{ stroke: 'var(--color-flag)' }} strokeWidth="1.8" />
    <text x="300" y="20" fontSize="14" fontWeight="700" style={{ fill: 'var(--color-flag)' }}>se cruzan</text>
  </g>
  <g transform="translate(0, 180)">
    <rect x={56} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-flag-soft)', stroke: 'var(--color-flag)' }} strokeWidth="1.5" strokeDasharray="4 3" />
    <text x={70} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'('}</text>
    <rect x={88} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={102} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'('}</text>
    <rect x={120} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={134} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{')'}</text>
    <path d="M 102 31 C 102 47, 134 47, 134 31" fill="none" style={{ stroke: 'var(--color-keep)' }} strokeWidth="1.8" />
    <path d="M 66 35 l 9 10 m -9 0 l 9 -10" fill="none" style={{ stroke: 'var(--color-flag)' }} strokeWidth="1.8" strokeLinecap="round" />
    <text x="300" y="20" fontSize="14" fontWeight="700" style={{ fill: 'var(--color-flag)' }}>quedó una sin cerrar</text>
  </g>
  <g transform="translate(0, 262)">
    <rect x={56} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={70} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'('}</text>
    <rect x={88} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
    <text x={102} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{')'}</text>
    <rect x={120} y="0" width="28" height="28" rx="3" style={{ fill: 'var(--color-flag-soft)', stroke: 'var(--color-flag)' }} strokeWidth="1.5" strokeDasharray="4 3" />
    <text x={134} y="20" textAnchor="middle" fontSize="17" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{')'}</text>
    <path d="M 70 31 C 70 47, 102 47, 102 31" fill="none" style={{ stroke: 'var(--color-keep)' }} strokeWidth="1.8" />
    <path d="M 130 35 l 9 10 m -9 0 l 9 -10" fill="none" style={{ stroke: 'var(--color-flag)' }} strokeWidth="1.8" strokeLinecap="round" />
    <text x="300" y="20" fontSize="14" fontWeight="700" style={{ fill: 'var(--color-flag)' }}>cierra una que nunca se abrió</text>
  </g>
</svg>

Balanceada quiere decir que **los arcos anidan**: ninguno cruza a otro y
ninguno queda suelto. Y de ahí salen las tres únicas formas de fallar —se
cruzan, queda algo abierto, o cierra algo que nunca se abrió—, que son las
tres que el algoritmo va a tener que detectar.

</Slide>

<Slide title="La pila lo ve">
Cada apertura se apila; cada cierre desapila y compara. La cima es siempre
**el último símbolo que quedó abierto**, que es exactamente con el que un
cierre tiene que corresponderse.

Tres auxiliares de una línea hacen el resto: `isOpener(c)` e `isCloser(c)`
dicen si el carácter abre o cierra, y `openerFor(c)` devuelve la apertura
que le toca a un cierre —`(` para `)`, `[` para `]`, `{` para `}`—. Los
escribimos completos al final.

<StepShow
  language="java"
  code={`for (char c : s.toCharArray()) {
    if (isOpener(c)) {
        pending.push(c);
    } else if (isCloser(c)) {
        if (pending.empty()) return false;
        if (pending.pop() != openerFor(c)) return false;
    }
}
return pending.empty();`}
>
  <Step lines={[]}>
    <Paso expr="({[]})" cursor={-1} pila={[]} nota="La pila arranca vacía y recorremos la expresión de izquierda a derecha." />
  </Step>
  <Step lines={[2, 3]}>
    <Paso expr="({[]})" cursor={0} pila={['(']} nota="( es una apertura: se apila." />
  </Step>
  <Step lines={[2, 3]}>
    <Paso expr="({[]})" cursor={1} pila={['(', '{']} nota="{ es una apertura: se apila encima." />
  </Step>
  <Step lines={[2, 3]}>
    <Paso expr="({[]})" cursor={2} pila={['(', '{', '[']} nota="[ es una apertura: se apila encima." />
  </Step>
  <Step lines={[4, 6]}>
    <Paso expr="({[]})" cursor={3} pila={['(', '{']} nota="] cierra. La cima era [, que es justo su pareja: se desapila." />
  </Step>
  <Step lines={[4, 6]}>
    <Paso expr="({[]})" cursor={4} pila={['(']} nota="} cierra. La cima era {, su pareja: se desapila." />
  </Step>
  <Step lines={[4, 6]}>
    <Paso expr="({[]})" cursor={5} pila={[]} nota=") cierra. La cima era (, su pareja: se desapila." />
  </Step>
  <Step lines={[9]}>
    <Paso expr="({[]})" cursor={-1} pila={[]} nota="Se acabó la expresión y la pila quedó vacía: está balanceada." />
  </Step>
</StepShow>

Los arcos de la lámina anterior son la pila: cada arco se abre cuando el
símbolo entra y se cierra cuando sale.

</Slide>

<Slide title="Cuando no está balanceada">
<StepShow
  language="java"
  code={`for (char c : s.toCharArray()) {
    if (isOpener(c)) {
        pending.push(c);
    } else if (isCloser(c)) {
        if (pending.empty()) return false;
        if (pending.pop() != openerFor(c)) return false;
    }
}
return pending.empty();`}
>
  <Step lines={[]}>
    <Paso expr="([)]" cursor={-1} pila={[]} nota="La misma cantidad de cada símbolo que la anterior. Lo que cambia es el orden." />
  </Step>
  <Step lines={[2, 3]}>
    <Paso expr="([)]" cursor={0} pila={['(']} nota="( es una apertura: se apila." />
  </Step>
  <Step lines={[2, 3]}>
    <Paso expr="([)]" cursor={1} pila={['(', '[']} nota="[ es una apertura: se apila encima." />
  </Step>
  <Step lines={[4, 6]}>
    <Paso expr="([)]" cursor={2} pila={['(', '[']} nota=") cierra, y su pareja es (. Pero la cima es [: el ) se cruza con él." falla />
  </Step>
</StepShow>

La pila detecta el cruce en el tercer carácter, sin haber leído el resto.
Y las tres formas de fallar son las tres preguntas del código: la línea 5
atrapa el cierre que no tiene nada abierto, la 6 atrapa el cruce, y la 9
atrapa lo que quedó abierto al final.

Una sola pasada, y la pila crece a lo más hasta la cantidad de aperturas:
$$\Theta(N)$$ de tiempo y $$\Theta(N)$$ de espacio en el peor caso.

</Slide>

<Slide title="El código completo">
<Exercise title="Paréntesis balanceados">

El algoritmo entero, con los tres auxiliares. Los casos de prueba son las
cuatro expresiones de la primera lámina, más la cadena vacía. Presiona
**Comprobar** para correrlo, y después cambia lo que quieras.

```java starter
import java.util.Stack;

class Solution {

    static boolean balanced(String s) {
        Stack<Character> pending = new Stack<>();
        for (char c : s.toCharArray()) {
            if (isOpener(c)) {
                pending.push(c);
            } else if (isCloser(c)) {
                if (pending.empty()) return false;
                if (pending.pop() != openerFor(c)) return false;
            }
        }
        return pending.empty();
    }

    static boolean isOpener(char c) {
        return c == '(' || c == '[' || c == '{';
    }

    static boolean isCloser(char c) {
        return c == ')' || c == ']' || c == '}';
    }

    static char openerFor(char c) {
        if (c == ')') return '(';
        if (c == ']') return '[';
        return '{';
    }
}
```

```java test
check(Solution.balanced("({[]})"), true);
check(Solution.balanced("([)]"), false);
check(Solution.balanced("(()"), false);
check(Solution.balanced("())"), false);
check(Solution.balanced(""), true);
```

</Exercise>
</Slide>

<Slide title="Notación polaca inversa">
En la notación polaca inversa el operador va **después** de sus dos
operandos. Eso suena a un detalle de escritura y no lo es: con los
operadores al final, una expresión no necesita paréntesis ni reglas de
precedencia, porque el orden ya dice qué se aplica a qué.

<svg viewBox="0 0 660 200" style={{ width: '100%', maxWidth: '660px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="A la izquierda, dos expresiones con paréntesis: abre tres más cuatro cierra por cinco, y tres más abre cuatro por cinco cierra. A la derecha, las mismas dos en polaca inversa: tres cuatro más cinco por, y tres cuatro cinco por más. Los mismos números en el mismo orden; lo único que cambia es dónde van los operadores, y no hace falta ningún paréntesis.">
  <defs>
    <marker id="rpn-punta" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-ink-faint)' }} />
    </marker>
  </defs>
  <text x="16" y="24" fontSize="13" fontWeight="700" style={{ fill: 'var(--color-ink-faint)' }}>con paréntesis</text>
  <text x="330" y="24" fontSize="13" fontWeight="700" style={{ fill: 'var(--color-accent)' }}>en polaca inversa</text>
  <text x="16" y="61" fontSize="17" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>(3 + 4) × 5</text>
  <path d="M 150 56 l 24 0" fill="none" style={{ stroke: 'var(--color-ink-faint)' }} strokeWidth="1.5" markerEnd="url(#rpn-punta)" />
  <rect x={330} y="40" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={347} y="61" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'3'}</text>
  <rect x={370} y="40" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={387} y="61" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'4'}</text>
  <rect x={410} y="40" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={427} y="61" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'+'}</text>
  <rect x={450} y="40" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={467} y="61" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'5'}</text>
  <rect x={490} y="40" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={507} y="61" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'*'}</text>
  <text x="16" y="107" fontSize="17" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>3 + (4 × 5)</text>
  <path d="M 150 102 l 24 0" fill="none" style={{ stroke: 'var(--color-ink-faint)' }} strokeWidth="1.5" markerEnd="url(#rpn-punta)" />
  <rect x={330} y="86" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={347} y="107" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'3'}</text>
  <rect x={370} y="86" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={387} y="107" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'4'}</text>
  <rect x={410} y="86" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={427} y="107" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'5'}</text>
  <rect x={450} y="86" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={467} y="107" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'*'}</text>
  <rect x={490} y="86" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={507} y="107" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'+'}</text>
  <text x="16" y="146" fontSize="13" style={{ fill: 'var(--color-ink-faint)' }}>los mismos números en el mismo orden, resultados distintos — y sin un paréntesis</text>
</svg>

Y evaluarla es ir reduciendo de izquierda a derecha: cada operador consume
los **dos resultados más recientes** y deja uno en su lugar.

<svg viewBox="0 0 660 190" style={{ width: '100%', maxWidth: '620px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="La expresión tres cuatro más cinco por, reduciéndose. Una llave abarca el tres, el cuatro y el más, y baja a un siete resaltado. Otra llave abarca ese siete, el cinco y el por, y baja a treinta y cinco. Cada operador consume los dos resultados más recientes.">
  <defs>
    <marker id="rpn-punta-ac" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  <rect x={40} y="20" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={57} y="41" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'3'}</text>
  <rect x={80} y="20" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={97} y="41" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'4'}</text>
  <rect x={120} y="20" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={137} y="41" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'+'}</text>
  <rect x={160} y="20" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={177} y="41" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'5'}</text>
  <rect x={200} y="20" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={217} y="41" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'*'}</text>
  <path d="M 44 56 l 0 10 l 112 0 l 0 -10" fill="none" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" />
  <path d="M 100 66 l 0 14" fill="none" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#rpn-punta-ac)" />
  <rect x={83} y="86" width="34" height="30" rx="3" style={{ fill: 'var(--color-mark-soft)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={100} y="107" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'7'}</text>
  <rect x={160} y="86" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={177} y="107" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'5'}</text>
  <rect x={200} y="86" width="34" height="30" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={217} y="107" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'*'}</text>
  <path d="M 87 122 l 0 10 l 190 0 l 0 -10" fill="none" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" />
  <path d="M 182 132 l 0 14" fill="none" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#rpn-punta-ac)" />
  <rect x={160} y="152" width="44" height="30" rx="3" style={{ fill: 'var(--color-mark-soft)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
  <text x={182} y="173" textAnchor="middle" fontSize="16" fontWeight="600" fontFamily="monospace" style={{ fill: 'var(--color-ink)' }}>{'35'}</text>
  <text x="240" y="174" fontSize="13" style={{ fill: 'var(--color-ink-faint)' }}>cada operador consume los dos resultados más recientes</text>
</svg>

«Los dos más recientes» es LIFO otra vez, y por eso el evaluador entero cabe
en una pila.

</Slide>

<Slide title="La pila lo evalúa">
Los números se apilan; cada operador desapila dos y apila el resultado. Al
final queda un solo valor, que es la respuesta.

<StepShow
  language="java"
  code={`for (String t : tokens) {
    if (isOperator(t)) {
        int b = values.pop();
        int a = values.pop();
        values.push(apply(a, b, t));
    } else {
        values.push(Integer.parseInt(t));
    }
}
return values.pop();`}
>
  <Step lines={[]}>
    <Paso expr="3 4 + 5 *" sep=" " cursor={-1} pila={[]} nota="La pila arranca vacía y leemos los tokens de izquierda a derecha." />
  </Step>
  <Step lines={[6, 7]}>
    <Paso expr="3 4 + 5 *" sep=" " cursor={0} pila={['3']} nota="3 es un número: se apila." />
  </Step>
  <Step lines={[6, 7]}>
    <Paso expr="3 4 + 5 *" sep=" " cursor={1} pila={['3', '4']} nota="4 es un número: se apila encima." />
  </Step>
  <Step lines={[2, 3, 4, 5]}>
    <Paso expr="3 4 + 5 *" sep=" " cursor={2} pila={['7']} nota="+ es operador: saca 4 y 3, y apila 3 + 4 = 7." />
  </Step>
  <Step lines={[6, 7]}>
    <Paso expr="3 4 + 5 *" sep=" " cursor={3} pila={['7', '5']} nota="5 es un número: se apila encima." />
  </Step>
  <Step lines={[2, 3, 4, 5]}>
    <Paso expr="3 4 + 5 *" sep=" " cursor={4} pila={['35']} nota="* es operador: saca 5 y 7, y apila 7 × 5 = 35." />
  </Step>
  <Step lines={[10]}>
    <Paso expr="3 4 + 5 *" sep=" " cursor={-1} pila={['35']} nota="Se acabaron los tokens y queda un solo valor: ése es el resultado." />
  </Step>
</StepShow>

`isOperator(t)` responde si el token es uno de los cuatro símbolos, y
`apply(a, b, t)` devuelve el resultado de aplicárselo a `a` y `b`. Los dos
están completos al final.

</Slide>

<Slide title="El orden de los dos pop">
La corrección del evaluador se juega en dos líneas consecutivas, y con la
resta se ve en un solo token.

<StepShow
  language="java"
  code={`for (String t : tokens) {
    if (isOperator(t)) {
        int b = values.pop();
        int a = values.pop();
        values.push(apply(a, b, t));
    } else {
        values.push(Integer.parseInt(t));
    }
}
return values.pop();`}
>
  <Step lines={[6, 7]}>
    <Paso expr="5 3 -" sep=" " cursor={1} pila={['5', '3']} nota="5 y 3 ya están apilados, en ese orden." />
  </Step>
  <Step lines={[3]}>
    <Paso expr="5 3 -" sep=" " cursor={2} pila={['5']} nota="El primer pop entrega 3: es el operando DERECHO, porque fue el último en entrar." />
  </Step>
  <Step lines={[4]}>
    <Paso expr="5 3 -" sep=" " cursor={2} pila={[]} nota="El segundo pop entrega 5: el operando izquierdo." />
  </Step>
  <Step lines={[5]}>
    <Paso expr="5 3 -" sep=" " cursor={2} pila={['2']} nota={'apply(5, 3, "−") apila 5 − 3 = 2.'} />
  </Step>
</StepShow>

Invierte esas dos líneas y el evaluador calcula `3 − 5 = −2`. Con la suma y
la multiplicación los dos órdenes dan lo mismo, así que el error sobrevive
las pruebas fáciles y aparece recién con una resta o una división.

</Slide>

<Slide title="El código completo">
<Exercise title="Notación polaca inversa">

El evaluador entero, con los dos auxiliares. Presiona **Comprobar** para
correrlo, y después cambia lo que quieras.

```java starter
import java.util.Stack;

class Solution {

    static int evalRPN(String[] tokens) {
        Stack<Integer> values = new Stack<>();
        for (String t : tokens) {
            if (isOperator(t)) {
                int b = values.pop();
                int a = values.pop();
                values.push(apply(a, b, t));
            } else {
                values.push(Integer.parseInt(t));
            }
        }
        return values.pop();
    }

    static boolean isOperator(String t) {
        return t.equals("+") || t.equals("-") || t.equals("*") || t.equals("/");
    }

    static int apply(int a, int b, String t) {
        if (t.equals("+")) return a + b;
        if (t.equals("-")) return a - b;
        if (t.equals("*")) return a * b;
        return a / b;
    }
}
```

```java test
check(Solution.evalRPN(new String[] {"3", "4", "+", "5", "*"}), 35);
check(Solution.evalRPN(new String[] {"3", "4", "5", "*", "+"}), 23);
check(Solution.evalRPN(new String[] {"5", "3", "-"}), 2);
check(Solution.evalRPN(new String[] {"8", "2", "/"}), 4);
check(Solution.evalRPN(new String[] {"7"}), 7);
```

</Exercise>
</Slide>

{/* Book-only: el contexto histórico se lee, no se proyecta. Sacarlo del */}
{/* <Slide> es lo que lo deja fuera del deck en modo explicit, y es lo que */}
{/* devuelve esta lámina a una escala legible (#294, revisión visual). */}

La notación se debe a Jan Łukasiewicz, que en 1924 propuso poner el
operador adelante; la variante inversa, con el operador al final, se
atribuye a Charles Hamblin y a Friedrich Bauer hacia fines de los años
cincuenta, ya pensando en cómo evaluar expresiones con una máquina.

<SectionBreak />

## Queue · Cola

<Slide title="Una cola">
Una **cola** es una colección con dos puertas, y cada una en un solo
sentido: se entra por el **final** y se sale por el **frente**. Los
elementos de adentro no se tocan.

<svg viewBox="70 20 560 150" style={{ width: '100%', maxWidth: '680px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Una cola de tres elementos, 3, 8 y 5, abierta por los dos extremos. Por la izquierda, rotulada frente, una flecha saca el 3: es dequeue. Por la derecha, rotulada final, otra flecha mete un 9 que espera afuera: es enqueue. Los dos extremos tienen un solo sentido.">
  <defs>
    <marker id="cola-intro-punta" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>

  <text x="150" y="48" textAnchor="middle" fontSize="14" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>dequeue() → 3</text>
  <line x1="200" y1="96" x2="112" y2="96" style={{ stroke: 'var(--color-accent)' }} strokeWidth="2.2" markerEnd="url(#cola-intro-punta)" />

  {[3, 8, 5].map((v, i) => (
    <g key={i}>
      <rect x={210 + i * 64} y="72" width="58" height="48" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <text x={239 + i * 64} y="104" textAnchor="middle" fontSize="21" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
    </g>
  ))}
  <text x="239" y="62" textAnchor="middle" fontSize="12" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>frente</text>
  <text x="367" y="62" textAnchor="middle" fontSize="12" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>final</text>
  <text x="303" y="146" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>se sale por aquí · se entra por allá</text>

  <text x="530" y="48" textAnchor="middle" fontSize="14" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>enqueue(9)</text>
  <line x1="496" y1="96" x2="410" y2="96" style={{ stroke: 'var(--color-accent)' }} strokeWidth="2.2" markerEnd="url(#cola-intro-punta)" />
  <rect x="502" y="72" width="58" height="48" rx="3" style={{ fill: 'var(--color-accent)', stroke: 'var(--color-accent)' }} strokeWidth="1.8" />
  <text x="531" y="104" textAnchor="middle" fontSize="21" fontWeight="600" style={{ fill: 'var(--color-on-accent)' }}>9</text>
</svg>

Las dos operaciones trabajan en extremos opuestos. `enqueue` (encolar) pone
un elemento nuevo en el final; `dequeue` (desencolar) saca el del frente y
lo devuelve. No hay otra forma de acceder a los elementos: si quieres sacar
el que está en el tercer lugar, tienes que sacar antes los dos que tiene
adelante.

Los elementos salen, entonces, en el mismo orden en que entraron. Esa regla
se llama **FIFO**, del inglés _first in, first out_: **el primero en entrar
es el primero en salir**.

</Slide>

<Slide title="Colas en la vida real">
Una cola no es una idea de la programación. Es la forma de cualquier espera
donde se atiende por orden de llegada.

- **La fila de una boletería.** El que llega se pone atrás, el que atienden
  es el de adelante, y nadie necesita saber cuántos hay ni en qué lugar
  está.
- **La cola de impresión.** Una impresora compartida recibe trabajos de
  varias personas a la vez e imprime de a uno, en el orden en que llegaron.
- **El búfer entre quien produce y quien consume.** El teclado genera
  pulsaciones más rápido de lo que el programa las procesa, y la tarjeta de
  red entrega paquetes cuando llegan y no cuando conviene. Entre los dos hay
  una cola, y su trabajo es que ninguno espere al otro.
- **Los pedidos de un servidor.** Llegan más rápido de lo que se atienden,
  y responderlos en desorden sería tratar distinto a dos clientes iguales.

Los cuatro hacen lo mismo con palabras distintas: poner atrás y atender
adelante. Ese comportamiento está definido con precisión en un **tipo de
dato abstracto** (TDA) llamado **Queue**.

</Slide>

<Slide title="El TDA Queue">
Una **cola** es una colección con dos extremos, cada uno con un solo
sentido: se entra por el **final** y se sale por el **frente**. Por eso lo
primero que entró es lo primero que sale.

<div style={{ border: '1px solid var(--color-rule)', borderRadius: '8px', overflow: 'hidden', margin: '1.25rem 0' }}>
  <div style={{ background: 'var(--color-sunk)', padding: '0.55rem 0.9rem', borderBottom: '1px solid var(--color-rule)', fontWeight: 700 }}>Queue <span style={{ fontWeight: 400, color: 'var(--color-ink-faint)' }}>· el contrato</span></div>
  <div style={{ display: 'grid', gridTemplateColumns: 'max-content 1fr', columnGap: '2rem' }}>
  <div style={{ gridColumn: '1 / -1', padding: '0.7rem 0.9rem 0.15rem', fontSize: '0.7rem', fontWeight: 700, letterSpacing: '0.08em', color: 'var(--color-ink-faint)' }}>CREAR</div>
  <code style={{ padding: `0.15rem 0 0.7rem 0.9rem`, whiteSpace: 'nowrap' }}>build()</code>
  <span style={{ padding: `0.15rem 0.9rem 0.7rem 0` }}>construye la cola sin elementos</span>
  <div style={{ gridColumn: '1 / -1', borderTop: '1px solid var(--color-rule)', padding: '0.7rem 0.9rem 0.15rem', fontSize: '0.7rem', fontWeight: 700, letterSpacing: '0.08em', color: 'var(--color-keep)' }}>CONSULTAR · no cambian nada</div>
  <code style={{ padding: `0.15rem 0 0.3rem 0.9rem`, whiteSpace: 'nowrap' }}>front() → elemento</code>
  <span style={{ padding: `0.15rem 0.9rem 0.3rem 0` }}>el que está en el frente · la cola no cambia</span>
  <code style={{ padding: `0.15rem 0 0.7rem 0.9rem`, whiteSpace: 'nowrap' }}>empty() → booleano</code>
  <span style={{ padding: `0.15rem 0.9rem 0.7rem 0` }}>si no hay ningún elemento</span>
  <div style={{ gridColumn: '1 / -1', borderTop: '1px solid var(--color-rule)', padding: '0.7rem 0.9rem 0.15rem', fontSize: '0.7rem', fontWeight: 700, letterSpacing: '0.08em', color: 'var(--color-accent)' }}>MODIFICAR · cambian el estado</div>
  <code style={{ padding: `0.15rem 0 0.3rem 0.9rem`, whiteSpace: 'nowrap' }}>enqueue(x)</code>
  <span style={{ padding: `0.15rem 0.9rem 0.3rem 0` }}>pone x al final · largo + 1</span>
  <code style={{ padding: `0.15rem 0 0.7rem 0.9rem`, whiteSpace: 'nowrap' }}>dequeue() → elemento</code>
  <span style={{ padding: `0.15rem 0.9rem 0.7rem 0` }}>saca el del frente y lo devuelve · largo − 1</span>
  </div>
  <div style={{ padding: '0.65rem 0.9rem', borderTop: '1px solid var(--color-rule)', background: 'var(--color-sunk)' }}>
    <div style={{ fontSize: '0.7rem', fontWeight: 700, letterSpacing: '0.08em', color: 'var(--color-ink-faint)' }}>LO QUE NO DICE</div>
    <div style={{ marginTop: '0.35rem' }}>Dónde viven los elementos, si están juntos o sueltos, ni cuánto cuesta cada operación. Tampoco hay manera de mirar el último que entró, ni de adelantar a nadie: el frente es el único elemento visible.</div>
  </div>
</div>

Cuatro operaciones y ninguna recibe una posición. Lo que el contrato fija es
**por dónde** se entra y por dónde se sale: **FIFO**, del inglés _first in,
first out_.

Pedirle el frente a una cola vacía es un error, no un valor. `front()` y
`dequeue()` tienen sentido únicamente cuando `empty()` es falso.

</Slide>

<Slide title="Cómo se implementa este TDA">
El contrato no dice dónde viven los elementos, pero sí pide algo exigente:
**los dos extremos**, y los dos baratos.

La cola se puede construir utilizando una de las dos estructuras de datos:

| Queue | Sobre arreglo dinámico | Sobre lista simple con `tail` |
|---|---|---|
| `enqueue(x)` | escribir en `data[size]`, subir `size` (crecer si está lleno) | `insertLast(x)` |
| `dequeue()` | leer `data[0]`, correr todo lo demás, bajar `size` | `deleteFirst()` |
| `front()` | leer `data[0]` | leer el valor de `head` |
| `empty()` | `size == 0` | `head == null` |

Una de esas cinco casillas no se parece a las otras. El arreglo sabe operar
barato en **un** extremo —el final—, y la cola necesita los dos: desencolar
le queda en el lado incómodo. La lista, en cambio, llega a los dos sin
caminar, y lo único que le hace falta es una referencia de más.

</Slide>

<Slide title="Queue sobre un arreglo dinámico">
La primera idea es la directa: el mismo arreglo dinámico de la pila,
con los elementos en un bloque consecutivo desde la casilla cero, el frente
en `data[0]` y el final en `data[size - 1]`.

Con esa disposición, encolar es escribir en la primera casilla libre y subir
`size`, que es la operación barata del arreglo. Pero desencolar saca el de
`data[0]`, y para que el frente vuelva a estar en la casilla cero hay que
**correr todos los demás una casilla a la izquierda**.

<svg viewBox="0 0 700 208" style={{ width: '100%', maxWidth: '700px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Un bloque de seis casillas con 3, 8 y 5 en las tres primeras. El 3 sale por arriba desde la casilla cero. Dos flechas curvas debajo del bloque llevan el 8 de la casilla uno a la cero y el 5 de la casilla dos a la uno.">
  <defs>
    <marker id="corrim-punta" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
    <marker id="corrim-punta-sale" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-ink-soft)' }} />
    </marker>
  </defs>
  <text x="70" y="20" textAnchor="middle" fontSize="13" style={{ fill: 'var(--color-ink-soft)' }}>sale el 3</text>
  <line x1="70" y1="54" x2="70" y2="30" style={{ stroke: 'var(--color-ink-soft)' }} strokeWidth="1.8" markerEnd="url(#corrim-punta-sale)" />
  {[0, 1, 2, 3, 4, 5].map((i) => (
    <g key={i}>
      <rect x={20 + i * 108} y="60" width="100" height="52" rx="3" style={{ fill: i < 3 ? 'var(--color-surface)' : 'var(--color-sunk)', stroke: i < 3 ? 'var(--color-rule-strong)' : 'var(--color-rule)' }} strokeWidth={i < 3 ? 1.6 : 1.2} strokeDasharray={i < 3 ? undefined : '4 3'} />
      <text x={70 + i * 108} y="94" textAnchor="middle" fontSize="22" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{['3', '8', '5', '', '', ''][i]}</text>
      <text x={70 + i * 108} y="132" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>{i}</text>
    </g>
  ))}
  {/* Las dos copias, cada una en su propio tramo. Antes compartían el punto
      178: la segunda terminaba exactamente donde empezaba la primera, así que
      su punta quedaba tapada y la de la primera parecía suelta. Ahora cada
      curva sale a la izquierda del centro de su casilla y llega a la derecha
      del centro de la siguiente, con 16px de aire entre las dos. */}
  <path d="M 170 140 L 170 168 L 82 168 L 82 148" fill="none" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" strokeLinejoin="round" markerEnd="url(#corrim-punta)" />
  <path d="M 278 140 L 278 168 L 190 168 L 190 148" fill="none" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" strokeLinejoin="round" markerEnd="url(#corrim-punta)" />
  <text x="350" y="198" textAnchor="middle" fontSize="13" style={{ fill: 'var(--color-ink-faint)' }}>una copia por cada elemento que queda · N − 1 en total</text>
</svg>

Ese corrimiento cuesta $$\Theta(N)$$, y no es un detalle de implementación
que se pueda pulir: es la consecuencia directa de haber decidido que el
frente vive en una casilla fija. Una cola que se usa de verdad desencola
tantas veces como encola, así que la mitad de sus operaciones serían caras.

Y el contrato no pide nada de esto: pide sacar **el del frente**, no sacar
el de la casilla cero. Es guardar la cola en un bloque de casillas
consecutivas lo que ata el frente a la casilla cero, y es esa atadura la que
cuesta $$\Theta(N)$$ cada vez.

</Slide>

<Slide title="enqueue sobre arreglo dinámico">
`enqueue` **es `insertLast` sobre un arreglo dinámico**: escribir el
elemento nuevo en la primera casilla libre y avanzar el final. En este
extremo la cola no inventa nada, le pone otro nombre a la operación que el
arreglo ya hacía barata.

<SequenceStepper
  eda="dynamic-array"
  capacity={4}
  operation="insert-last"
  values={[3, 8]}
  value={[5, 9, 4, 23]}
  pointer="rear"
  showCode={false}
/>

En el **peor caso** `enqueue` cuesta $$\Theta(N)$$: cuando el bloque está
lleno hay que reservar uno del doble y copiar los $$N$$ elementos antes de
poder escribir. Cualquier otra llamada escribe en una casilla libre y sube
`size`, que es $$\Theta(1)$$.

El costo **amortizado** es $$\Theta(1)$$: como la capacidad se duplica,
entre dos llamadas caras hay $$N$$ baratas, así que el precio de la copia
repartido entre todas ellas es constante por llamada.

</Slide>

<Slide title="dequeue sobre arreglo dinámico">
`dequeue` **es `deleteFirst` sobre un arreglo dinámico**: devolver el de la
casilla cero y correr a todos los demás una casilla a la izquierda, para que
el frente vuelva a estar donde estaba.

<SequenceStepper
  eda="dynamic-array"
  capacity={8}
  operation="remove-first"
  values={[3, 8, 5, 9, 4]}
  times={3}
  pointer="front"
  showCode={false}
/>

Mira cómo cambia el trabajo entre una llamada y la siguiente: el primer
`dequeue` copió cuatro elementos, el segundo tres, el tercero dos. Con
$$N$$ elementos son $$N - 1$$ copias, y por eso desencolar cuesta
$$\Theta(N)$$.

Y acá no hay amortizado que valga. En `push` la corrida cara era **una cada
$$N$$**, y las baratas la pagaban; el corrimiento ocurre en **cada** llamada,
así que no hay nada entre quién repartirlo: $$\Theta(N)$$ en el peor caso y
$$\Theta(N)$$ amortizado.

</Slide>

<Slide title="Costos de Queue sobre arreglo dinámico">
| Operación | Peor caso | Amortizado | Invariante que restaura |
|---|---|---|---|
| `front()` | $$\Theta(1)$$ | $$\Theta(1)$$ | Ninguna: lee `data[0]` y no cambia nada |
| `empty()` | $$\Theta(1)$$ | $$\Theta(1)$$ | Ninguna: `size` ya guarda el dato |
| `size()` | $$\Theta(1)$$ | $$\Theta(1)$$ | Ninguna: devuelve el campo que las otras mantienen |
| `enqueue(x)` | $$\Theta(N)$$ | $$\Theta(1)$$ | Los elementos ocupan `size` casillas seguidas: se escribe en la primera libre, y el bloque crece si no hay |
| `dequeue()` | <span style={{ color: 'var(--color-flag)', fontWeight: 700 }}>$$\Theta(N)$$</span> | <span style={{ color: 'var(--color-flag)', fontWeight: 700 }}>$$\Theta(N)$$</span> | **El frente vive en `data[0]`**: para restaurarla hay que correr a todos los demás |

`dequeue` cuesta $$\Theta(N)$$, y ese costo **no se amortiza**. La llamada
cara de `enqueue` ocurre una cada $$N$$ y las $$N-1$$ baratas la pagan; el
corrimiento ocurre en **cada** llamada, así que no hay entre quién
repartirlo.

Una cola que se usa de verdad desencola tantas veces como encola, así que la
mitad de sus operaciones serían caras. **El arreglo es la estructura
equivocada para este contrato.**

</Slide>

<Slide title="Queue sobre una lista simplemente enlazada con tail">
La mejor forma de implementar una cola es una **lista simplemente enlazada
con `tail`**: se entra por `tail` y se sale por `head`, y ninguna de las dos
operaciones recorre nada.

<svg viewBox="0 0 700 182" style={{ width: '100%', maxWidth: '700px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Una lista enlazada de tres nodos con los valores 3, 8 y 5. La referencia head apunta al primero y está rotulada como el frente; la referencia tail apunta al último y está rotulada como el final; el último nodo apunta a null.">
  <defs>
    <marker id="cola-lista-punta" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-ink-soft)' }} />
    </marker>
    <marker id="cola-lista-punta-ref" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  <text x="46" y="30" textAnchor="middle" fontSize="15" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>head</text>
  <text x="46" y="50" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>el frente</text>
  <line x1="46" y1="58" x2="46" y2="88" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#cola-lista-punta-ref)" />
  <text x="426" y="30" textAnchor="middle" fontSize="15" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>tail</text>
  <text x="426" y="50" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>el final</text>
  <line x1="426" y1="58" x2="426" y2="88" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#cola-lista-punta-ref)" />
  {[3, 8, 5].map((v, i) => (
    <g key={i}>
      <rect x={20 + i * 190} y="92" width="86" height="54" rx="4" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <rect x={106 + i * 190} y="92" width="38" height="54" rx="4" style={{ fill: 'var(--color-sunk)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <text x={63 + i * 190} y="127" textAnchor="middle" fontSize="24" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
      <circle cx={125 + i * 190} cy="119" r="4" style={{ fill: 'var(--color-ink-soft)' }} />
      <line x1={125 + i * 190} y1="119" x2={i < 2 ? 200 + i * 190 : 620} y2="119" style={{ stroke: 'var(--color-ink-soft)' }} strokeWidth="1.6" markerEnd="url(#cola-lista-punta)" />
    </g>
  ))}
  <text x="638" y="124" fontSize="15" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>null</text>
  <text x="358" y="172" textAnchor="middle" fontSize="13" style={{ fill: 'var(--color-ink-faint)' }}>3 entró primero · 5 entró último</text>
</svg>

`head` entrega el frente sin caminar, que es lo que la lista ya daba. `tail`
entrega el final sin caminar, que es lo que la cola necesitaba: sin él,
encolar tendría que recorrer la cadena entera para encontrar al último y
costaría $$\Theta(N)$$.

Lo que se paga son dos referencias que mantener al día en vez de una,
incluida la cola de un solo nodo, donde las dos señalan al mismo.

```java
class Queue {
    private Node head;  // el primer nodo: el frente
    private Node tail;  // el último nodo: el final
    private int size;   // cuántos elementos hay

    void enqueue(int x)  // pone x en el final
    int dequeue()        // saca el del frente y lo devuelve
    int front()          // el del frente, sin sacarlo
    boolean empty()      // si la cola no tiene ningún elemento
    int size()           // cuántos elementos tiene
}
```

</Slide>

<Slide title="Las invariantes de la cola sobre lista">
Cuatro propiedades, y la cuarta es la que `tail` trae consigo.

1. **`head` apunta al primer nodo —el frente—, o a `null` si la cola está
   vacía.**

2. **Desde `head`, siguiendo `next`, se llega a todos los nodos y se
   termina en `null`.**

3. **`size` es la cantidad de nodos de esa cadena.**

4. **`tail` apunta al último nodo de la cadena, o a `null` si está
   vacía.** Si queda apuntando a un nodo que ya salió de la cola, el
   siguiente `enqueue` enlaza contra un nodo inalcanzable y el elemento
   nuevo nace perdido.

Las tres primeras son las de cualquier lista enlazada. La cuarta es el
precio de haber ganado el segundo extremo, y es una invariante de las que
cuestan cuidado y no tiempo: mantenerla no alarga ninguna operación, pero
olvidarla en un solo caso de borde rompe la estructura en silencio.

</Slide>

<Slide title="Las consultas">
Las tres operaciones que no cambian la cola salen de los atributos, sin
tocar la cadena.

```java
boolean empty() {
    return size == 0;
}

int size() {
    return size;
}

int front() {
    if (head == null) {
        throw new NoSuchElementException();
    }
    return head.value;
}
```

- **`empty()`** dice si la cola no tiene elementos. Lee un campo: $$\Theta(1)$$.
- **`size()`** devuelve cuántos hay. Lee el campo que `enqueue` y `dequeue`
  mantienen al día, así que nunca hay que recorrer la cadena para contarlo:
  $$\Theta(1)$$.
- **`front()`** devuelve el valor del frente sin sacarlo. `head` ya apunta
  ahí: $$\Theta(1)$$.

`front()` cae sobre `head` porque es la única referencia que se tiene sin
caminar. **Lo que hace de esto una cola y no una pila no son las consultas
sino dónde entra lo nuevo**: en el final, con `tail`, y no en la cabeza.

</Slide>

<Slide title="Enqueue">
Implementar `enqueue` es **usar `insertLast` sobre la misma lista**: poner el
elemento nuevo al final y mover `tail` hasta él.

<svg viewBox="30 76 600 150" style={{ width: '100%', maxWidth: '660px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Dos dibujos de la misma cola, abierta por los dos extremos: se sale por el frente, a la izquierda, y se entra por el final, a la derecha. A la izquierda, antes: 3, 8 y 5. Una flecha rotulada enqueue de 9 lleva al de la derecha, después, donde el 9 quedó al final, resaltado, y el frente sigue siendo el 3.">
  <defs>
    <marker id="cola-enq-flecha" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  {[
    { x0: 40, vals: [3, 8, 5], nuevo: -1, cap: 'antes' },
    { x0: 372, vals: [3, 8, 5, 9], nuevo: 3, cap: 'después' },
  ].map(({ x0, vals, nuevo, cap }) => (
    <g key={x0}>
      {vals.map((v, i) => (
        <g key={i}>
          <rect x={x0 + i * 52} y="110" width="48" height="44" rx="3" style={{ fill: i === nuevo ? 'var(--color-accent)' : 'var(--color-surface)', stroke: i === nuevo ? 'var(--color-accent)' : 'var(--color-rule-strong)' }} strokeWidth={i === nuevo ? 1.8 : 1.6} />
          <text x={x0 + i * 52 + 24} y="140" textAnchor="middle" fontSize="20" fontWeight="600" style={{ fill: i === nuevo ? 'var(--color-on-accent)' : 'var(--color-ink)' }}>{v}</text>
        </g>
      ))}
      <text x={x0 + 24} y="102" textAnchor="middle" fontSize="11" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>frente</text>
      <text x={x0 + (vals.length - 1) * 52 + 24} y="102" textAnchor="middle" fontSize="11" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>final</text>
      <text x={x0 + (vals.length * 52 - 4) / 2} y="176" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>{cap}</text>
    </g>
  ))}
  <text x="316" y="124" textAnchor="middle" fontSize="14" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>enqueue(9)</text>
  <line x1="248" y1="136" x2="362" y2="136" style={{ stroke: 'var(--color-accent)' }} strokeWidth="2.2" markerEnd="url(#cola-enq-flecha)" />
</svg>

```java
void enqueue(int x) {
    Node fresh = new Node(x);
    if (head == null) {
        head = fresh;
    } else {
        tail.next = fresh;
    }
    tail = fresh;
    size++;
}
```

El método crea el nodo, lo engancha al final de la cadena, mueve `tail`
hasta él y sube `size`. Engancharlo es una sola asignación —`tail.next =
fresh`— porque `tail` ya tenía al último en la mano; sin esa referencia
habría que recorrer la cadena entera para encontrarlo. Nada de esto depende
del largo: $$\Theta(1)$$.

El `if` es un **caso borde**: la cola vacía. Ahí no hay un último al que
engancharse, así que lo que se asigna es `head`. En las dos ramas el nodo
nuevo termina siendo el último, y por eso `tail = fresh` va afuera del `if`
y se escribe una sola vez.

</Slide>

<Slide title="Enqueue">
<SequenceStepper
  eda="linked-list-singly"
  operation="insert-last"
  values={[]}
  value={[3, 8, 5]}
  tail
  method="enqueue"
  receiver="cola"
  receiverType="Queue"
/>

</Slide>

<Slide title="Dequeue">
Implementar `dequeue` es **usar `deleteFirst`**: sacar el primer nodo de la
cadena y devolver su valor.

<svg viewBox="30 76 600 150" style={{ width: '100%', maxWidth: '660px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Dos dibujos de la misma cola. A la izquierda, antes: 3, 8, 5 y 9, con el 3 en el frente dibujado atenuado y punteado porque es el que sale. Una flecha rotulada dequeue devuelve 3 lleva al de la derecha, después, donde quedan 8, 5 y 9 y el frente pasó a ser el 8.">
  <defs>
    <marker id="cola-deq-flecha" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  {[
    { x0: 40, vals: [3, 8, 5, 9], sale: 0, cap: 'antes' },
    { x0: 372, vals: [8, 5, 9], sale: -1, cap: 'después' },
  ].map(({ x0, vals, sale, cap }) => (
    <g key={x0}>
      {vals.map((v, i) => (
        <g key={i}>
          <rect x={x0 + i * 52} y="110" width="48" height="44" rx="3" opacity={i === sale ? 0.55 : 1} style={{ fill: i === sale ? 'var(--color-sunk)' : 'var(--color-surface)', stroke: i === sale ? 'var(--color-rule)' : 'var(--color-rule-strong)' }} strokeWidth="1.6" strokeDasharray={i === sale ? '5 4' : undefined} />
          <text x={x0 + i * 52 + 24} y="140" textAnchor="middle" fontSize="20" fontWeight="600" opacity={i === sale ? 0.55 : 1} style={{ fill: 'var(--color-ink)' }}>{v}</text>
        </g>
      ))}
      <text x={x0 + (sale === 0 ? 52 : 0) + 24} y="102" textAnchor="middle" fontSize="11" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>frente</text>
      <text x={x0 + (vals.length - 1) * 52 + 24} y="102" textAnchor="middle" fontSize="11" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>final</text>
      <text x={x0 + (vals.length * 52 - 4) / 2} y="176" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>{cap}</text>
    </g>
  ))}
  <text x="316" y="124" textAnchor="middle" fontSize="14" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>dequeue() → 3</text>
  <line x1="300" y1="136" x2="362" y2="136" style={{ stroke: 'var(--color-accent)' }} strokeWidth="2.2" markerEnd="url(#cola-deq-flecha)" />
</svg>

```java
int dequeue() {
    if (head == null) {
        throw new NoSuchElementException();
    }
    int x = head.value;
    head = head.next;
    if (head == null) {
        tail = null;
    }
    size--;
    return x;
}
```

El método guarda el valor del frente, mueve `head` al segundo nodo y baja
`size`. El que sale queda sin nadie apuntándolo, y el colector de basura lo
recupera más tarde. Nada de esto depende del largo: $$\Theta(1)$$.

Los dos `if` son **casos borde**. El primero es la cola vacía: no hay frente
que devolver, así que lanza. El segundo es la cola de un solo nodo: al salir
él, `head` queda en `null` y `tail` tiene que acompañarlo, porque si se
queda apuntando al nodo que ya salió, el próximo `enqueue` engancharía el
elemento nuevo a un nodo que no pertenece a la cola.

</Slide>

<Slide title="Dequeue">
<SequenceStepper
  eda="linked-list-singly"
  operation="remove-first"
  values={[3, 8, 5]}
  times={3}
  tail
  method="dequeue"
  receiver="cola"
  receiverType="Queue"
/>

</Slide>

<Slide title="Costos de Queue sobre lista">
| Operación | Peor caso | Invariante que restaura |
|---|---|---|
| `build()` | $$\Theta(1)$$ | Deja `head` y `tail` en `null` y `size` en cero |
| `front()` | $$\Theta(1)$$ | Ninguna: lee `head.value` y no cambia la cadena |
| `empty()` | $$\Theta(1)$$ | Ninguna: `size` ya guarda el dato |
| `size()` | $$\Theta(1)$$ | Ninguna: devuelve el campo que las otras mantienen |
| `enqueue(x)` | $$\Theta(1)$$ | `tail` apunta al último: el que lo era enlaza al nuevo, y `tail` lo sigue |
| `dequeue()` | $$\Theta(1)$$ | `head` apunta al frente: pasa a ser el segundo, y si no queda ninguno `tail` lo acompaña |

Todas las operaciones son constantes, y por una sola razón: `head` y `tail`
dejan los dos extremos en la mano, así que ninguna tiene que caminar la
cadena para llegar a donde opera.

Llevar dos referencias en vez de una no cuesta tiempo, cuesta **cuidado**.
`dequeue` necesita un `if` que no hace nada mientras la cola tenga varios
elementos, y que lo es todo cuando sale el último: sin él, `tail` queda
apuntando a un nodo que ya salió de la cola.

</Slide>

<Slide title="Arreglo vs lista">
| | Sobre arreglo dinámico | Lista con `head` y `tail` |
|---|---|---|
| `enqueue(x)` | $$\Theta(1)$$ amortizado | $$\Theta(1)$$ siempre |
| `dequeue()` | <span style={{ color: 'var(--color-flag)', fontWeight: 700 }}>$$\Theta(N)$$</span> | $$\Theta(1)$$ |
| `front()`, `empty()` | $$\Theta(1)$$ | $$\Theta(1)$$ |
| memoria por elemento | el valor | el valor **y** un enlace |
| capacidad | crece copiando | sin límite previo |
| lo que hay que sostener | un índice | dos referencias y el caso de un nodo |

Acá no hay elección. La pila podía construirse igual de bien sobre las dos
estructuras y la decisión se jugaba en memoria y localidad; la cola no: el
arreglo pierde una fila entera, y es la fila de la operación que una cola
hace la mitad del tiempo.

Y la razón es geométrica, no de implementación. **Una pila toca un solo
extremo y un arreglo tiene uno barato.** Una cola toca los dos, y el arreglo
sólo tiene barato el final. La lista paga un enlace por elemento y a cambio
tiene los dos extremos en la mano.

Por eso, cuando alguien escribe una cola, la escribe sobre una lista.

</Slide>

<Slide title="Queue en Java">
Java no trae una clase `Queue`: trae una **interfaz** con ese nombre, que
fija las operaciones sin decir dónde se guardan los elementos. Quien escribe
el programa elige la clase que la implementa, y aquí usaremos `LinkedList`:
la lista enlazada con `tail`. Los nombres del contrato cambian:

| El TDA | Java |
|---|---|
| `enqueue(x)` | `add(x)` |
| `dequeue()` | `remove()` |
| `front()` | `element()` |
| `empty()` | `isEmpty()` |
| `size()` | `size()` |

```java
Queue<Integer> queue = new LinkedList<>();
queue.add(3);
queue.add(8);

int frente = queue.element();  // 3, y la cola queda igual
int x = queue.remove();        // 3, y ahora la cola sólo tiene el 8
```

A la izquierda del `=` va la interfaz y a la derecha la clase: la variable se
declara `Queue` y no `LinkedList`. Así el resto del programa sólo puede pedir
las operaciones del contrato, y cambiar la implementación es cambiar una
línea.

Sobre una cola vacía, `remove()` y `element()` lanzan
`NoSuchElementException`: no devuelven cero ni ninguna otra cosa. El trío
`offer`/`poll`/`peek` hace lo mismo devolviendo `false` o `null` en vez de
lanzar.

</Slide>

<Slide title="El problema de Josefo">
Hay $$n$$ personas en círculo, numeradas de 1 a $$n$$. Se cuenta de a $$k$$
y la $$k$$-ésima sale; el conteo sigue desde la siguiente, dando vueltas,
hasta que queda una sola. La pregunta es qué número sobrevive.

<svg viewBox="20 20 620 236" style={{ width: '100%', maxWidth: '660px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="A la izquierda, cinco personas numeradas del 1 al 5 en círculo; se cuenta de a dos desde el 1, y el 2 queda marcado como el que sale. A la derecha, las mismas cinco en una cola: el 1 sale por el frente y vuelve a entrar por el final, y entonces el 2 queda en el frente y es el que se va. El círculo y la cola son la misma cosa.">
  <defs>
    <marker id="jos-punta" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>

  <text x="140" y="42" textAnchor="middle" fontSize="13" fontWeight="700" style={{ fill: 'var(--color-ink-faint)' }}>el círculo</text>
  {[1, 2, 3, 4, 5].map((v, i) => {
    const a = -Math.PI / 2 + (i * 2 * Math.PI) / 5;
    const cx = 140 + 68 * Math.cos(a);
    const cy = 140 + 68 * Math.sin(a);
    const sale = v === 2;
    return (
      <g key={v}>
        <circle cx={cx} cy={cy} r="21" style={{ fill: sale ? 'var(--color-flag-soft)' : 'var(--color-surface)', stroke: sale ? 'var(--color-flag)' : 'var(--color-rule-strong)' }} strokeWidth="1.8" />
        <text x={cx} y={cy + 6} textAnchor="middle" fontSize="17" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
      </g>
    );
  })}
  <text x="140" y="250" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>se cuenta de a 2: sale el 2</text>

  <text x="430" y="42" textAnchor="middle" fontSize="13" fontWeight="700" style={{ fill: 'var(--color-accent)' }}>la misma vuelta, en una cola</text>
  {[1, 2, 3, 4, 5].map((v, i) => (
    <g key={v}>
      <rect x={300 + i * 54} y="112" width="48" height="44" rx="3" style={{ fill: v === 2 ? 'var(--color-flag-soft)' : 'var(--color-surface)', stroke: v === 2 ? 'var(--color-flag)' : 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <text x={324 + i * 54} y="142" textAnchor="middle" fontSize="19" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
    </g>
  ))}
  <text x="324" y="104" textAnchor="middle" fontSize="11" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>frente</text>
  <text x="540" y="104" textAnchor="middle" fontSize="11" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>final</text>
  <path d="M 316 168 l 0 26 l 232 0 l 0 -26" fill="none" style={{ stroke: 'var(--color-accent)' }} strokeWidth="2" markerEnd="url(#jos-punta)" />
  <text x="432" y="212" textAnchor="middle" fontSize="12" style={{ fill: 'var(--color-accent)' }}>pasar de largo = salir por el frente y volver por el final</text>
</svg>

Una cola representa el círculo de manera casi literal: el frente es a quien
le toca contar, y **volver al círculo es encolarse de nuevo atrás**. Con
eso, contar de a `k` es pasar de largo `k - 1` veces, y la `k`-ésima
simplemente no vuelve.

</Slide>

<Slide title="Una ronda completa">
Con $$n = 5$$ y $$k = 2$$: cada vuelta es un `dequeue` que vuelve a encolar,
y uno que no.

<StepShow
  language="java"
  code={`for (int i = 1; i < k; i++) {
    round.enqueue(round.dequeue());
}
round.dequeue();`}
>
  <Step lines={[]}>
    <Paso pila={['1', '2', '3', '4', '5']} etiqueta="cola" nota="Cinco en círculo, y contamos de a 2. El frente es a quien le toca." />
  </Step>
  <Step lines={[1, 2, 3]}>
    <Paso pila={['2', '3', '4', '5', '1']} etiqueta="cola" nota="El 1 cuenta «uno» y vuelve al final: pasa de largo." />
  </Step>
  <Step lines={[4]}>
    <Paso pila={['3', '4', '5', '1']} etiqueta="cola" nota="El 2 cuenta «dos»: sale. Quedan cuatro." />
  </Step>
  <Step lines={[1, 2, 3]}>
    <Paso pila={['4', '5', '1', '3']} etiqueta="cola" nota="El 3 pasa de largo." />
  </Step>
  <Step lines={[4]}>
    <Paso pila={['5', '1', '3']} etiqueta="cola" nota="El 4 sale. Quedan tres." />
  </Step>
  <Step lines={[1, 2, 3]}>
    <Paso pila={['1', '3', '5']} etiqueta="cola" nota="El 5 pasa de largo." />
  </Step>
  <Step lines={[4]}>
    <Paso pila={['3', '5']} etiqueta="cola" nota="El 1 sale. Quedan dos." />
  </Step>
  <Step lines={[1, 2, 3]}>
    <Paso pila={['5', '3']} etiqueta="cola" nota="El 3 pasa de largo." />
  </Step>
  <Step lines={[4]}>
    <Paso pila={['3']} etiqueta="cola" nota="El 5 sale. Sobrevive el 3." />
  </Step>
</StepShow>

La línea que resuelve el problema es `round.enqueue(round.dequeue())`: saca
del frente y devuelve al final, que es exactamente «pasar de largo».

Cada eliminación cuesta $$k - 1$$ pases de dos operaciones cada uno, más el
`dequeue` que saca: $$2k - 1$$. Hay $$n - 1$$ eliminaciones, así que el
total es $$(n-1)(2k-1)$$, es decir $$\Theta(nk)$$.

</Slide>

<Slide title="El conteo que se corre en uno">
El error fácil está en el `for`: pasar de largo `k` veces en vez de
`k - 1`. Con $$k = 2$$ se ve en la segunda vuelta.

<StepShow
  language="java"
  code={`for (int i = 1; i <= k; i++) {
    round.enqueue(round.dequeue());
}
round.dequeue();`}
>
  <Step lines={[1, 2, 3]}>
    <Paso pila={['1', '2', '3', '4', '5']} etiqueta="cola" nota="Con el conteo mal escrito, i va de 1 hasta k: son DOS pases, no uno." />
  </Step>
  <Step lines={[1, 2, 3]}>
    <Paso pila={['2', '3', '4', '5', '1']} etiqueta="cola" nota="El 1 pasa de largo. Hasta aquí igual." />
  </Step>
  <Step lines={[1, 2, 3]}>
    <Paso pila={['3', '4', '5', '1', '2']} etiqueta="cola" nota="El 2 también pasa de largo — y era el que tenía que salir." />
  </Step>
  <Step lines={[4]}>
    <Paso pila={['4', '5', '1', '2']} etiqueta="cola" nota="Sale el 3. El conteo se corrió en uno y la respuesta ya es otra." falla />
  </Step>
</StepShow>

El que sale es el 3 y tenía que ser el 2. La cuenta correcta es: para que
salga el `k`-ésimo hay que dejar pasar a los `k - 1` que van antes.

</Slide>

<Slide title="El código completo">
<Exercise title="El problema de Josefo">

El algoritmo entero, con la cola escrita a mano sobre un arreglo. Los casos
de prueba incluyen la ronda de la lámina anterior.

```java starter
class Solution {

    static int josephus(int n, int k) {
        int[] round = new int[2 * n * k + 1];
        int front = 0;
        int rear = 0;
        for (int i = 1; i <= n; i++) {
            round[rear++] = i;
        }
        int alive = n;
        while (alive > 1) {
            for (int i = 1; i < k; i++) {
                round[rear++] = round[front++];
            }
            front++;
            alive--;
        }
        return round[front];
    }
}
```

```java test
check(Solution.josephus(5, 2), 3);
check(Solution.josephus(1, 3), 1);
check(Solution.josephus(7, 1), 7);
check(Solution.josephus(4, 2), 1);
```

</Exercise>
</Slide>

<Slide title="Promedio móvil">
Llegan números de a uno, sin que se sepa cuántos serán. Después de cada
número hay que responder el promedio de los últimos $$K$$ — y hay que
hacerlo sin recorrer nada.

<svg viewBox="20 24 620 150" style={{ width: '100%', maxWidth: '660px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Una secuencia de números 1, 2, 3, 4, 5 que sigue llegando. Un recuadro abarca tres de ellos: es la ventana de tamaño K igual a 3. Al llegar el 4 la ventana se corre una posición, el 1 sale por la izquierda y el 4 entra por la derecha.">
  <defs>
    <marker id="pm-punta" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto">
      <path d="M0,0 L8,4 L0,8 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  {[1, 2, 3, 4, 5].map((v, i) => (
    <g key={v}>
      <rect x={90 + i * 66} y="70" width="56" height="46" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <text x={118 + i * 66} y="101" textAnchor="middle" fontSize="20" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
    </g>
  ))}
  <text x="420" y="101" fontSize="20" style={{ fill: 'var(--color-ink-faint)' }}>…</text>
  <rect x="82" y="62" width="200" height="62" rx="5" fill="none" style={{ stroke: 'var(--color-accent)' }} strokeWidth="2.4" />
  <text x="182" y="52" textAnchor="middle" fontSize="13" fontWeight="700" style={{ fill: 'var(--color-accent)' }}>la ventana · K = 3</text>
  <path d="M 100 134 l 0 14 l 214 0 l 0 -14" fill="none" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#pm-punta)" />
  <text x="340" y="152" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>cuando llega el 4, la ventana se corre: sale el 1, entra el 4</text>
</svg>

La cola **es** la ventana: cada número entra por el final, y cuando ya hay
$$K$$ el más viejo sale por el frente. Eso es exactamente lo que el contrato
de la cola entrega, y es lo que hace constante a la operación: el elemento
que hay que descontar es siempre el más viejo, y no hay que buscarlo.

</Slide>

<Slide title="La ventana avanza">
La suma se mantiene al día: se le suma el que entra y se le resta el que
sale. Así el promedio es una división y nunca un recorrido.

<StepShow
  language="java"
  code={`double next(int x) {
    window.enqueue(x);
    sum += x;
    length++;
    if (length > capacity) {
        sum -= window.dequeue();
        length--;
    }
    return (double) sum / length;
}`}
>
  <Step lines={[]}>
    <Paso pila={[]} etiqueta="ventana" nota="K = 3. La ventana arranca vacía, con suma 0." />
  </Step>
  <Step lines={[1, 2, 3, 9]}>
    <Paso pila={['1']} etiqueta="ventana" nota="Entra 1. Suma 1, largo 1 → promedio 1,00." />
  </Step>
  <Step lines={[1, 2, 3, 9]}>
    <Paso pila={['1', '2']} etiqueta="ventana" nota="Entra 2. Suma 3, largo 2 → promedio 1,50." />
  </Step>
  <Step lines={[1, 2, 3, 9]}>
    <Paso pila={['1', '2', '3']} etiqueta="ventana" nota="Entra 3. Suma 6, largo 3 → promedio 2,00. La ventana está llena." />
  </Step>
  <Step lines={[1, 2, 3]}>
    <Paso pila={['1', '2', '3', '4']} etiqueta="ventana" nota="Entra 4: la ventana se pasó de K." />
  </Step>
  <Step lines={[5, 6, 7]}>
    <Paso pila={['2', '3', '4']} etiqueta="ventana" nota="Sale el más viejo por el frente, el 1, y la suma le resta 1. Queda 9, largo 3 → promedio 3,00." />
  </Step>
</StepShow>

El último paso es el único interesante: la ventana ya estaba llena, así que
la suma se ajustó en las dos direcciones —sumó 4 y restó 1— sin mirar los
elementos de adentro.

</Slide>

<Slide title="El código completo">
<Exercise title="Promedio móvil">

La estructura entera. `capacity` es el $$K$$ de la ventana, y el
constructor lo rechaza si no es positivo: una ventana de tamaño cero
dividiría cero por cero y devolvería `NaN` en cada llamada, que es la peor
manera de fallar —sin ruido—. Mientras la ventana no llega a $$K$$
responde el promedio de lo que tenga, en vez de negarse.

```java starter
import java.util.LinkedList;
import java.util.Queue;

class Solution {

    static double[] movingAverage(int k, int[] stream) {
        if (k <= 0) {
            throw new IllegalArgumentException();
        }
        Queue<Integer> window = new LinkedList<>();
        double[] out = new double[stream.length];
        int sum = 0;
        for (int i = 0; i < stream.length; i++) {
            window.add(stream[i]);
            sum += stream[i];
            if (window.size() > k) {
                sum -= window.remove();
            }
            out[i] = (double) sum / window.size();
        }
        return out;
    }
}
```

```java test
double[] r = Solution.movingAverage(3, new int[] {1, 2, 3, 4});
check(r.length, 4);
check(r[0], 1.0);
check(r[1], 1.5);
check(r[2], 2.0);
check(r[3], 3.0);
check(Solution.movingAverage(1, new int[] {7, 9})[1], 9.0);
check(Solution.movingAverage(5, new int[] {2, 4})[1], 3.0);
```

</Exercise>
</Slide>

<SectionBreak />

## Deque · dos puntas abiertas

<Slide title="Las cuatro operaciones de un deque">
Un **deque** —de _double-ended queue_— abre los dos extremos en los dos
sentidos: se puede entrar y salir tanto por el frente como por el final.

<svg viewBox="0 0 700 200" style={{ width: '100%', maxWidth: '700px', height: 'auto', display: 'block', margin: '1.25rem auto' }} role="img" aria-label="Cuatro elementos en fila, 7, 3, 1 y 5. A la izquierda, una flecha rotulada pushFront entra por el frente y otra rotulada popFront sale por el frente. A la derecha, una flecha rotulada pushBack entra por el final y otra rotulada popBack sale por el final. El extremo izquierdo está rotulado «el frente» y el derecho «el final».">
  <defs>
    <marker id="deque-punta" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto">
      <path d="M0,0 L7,3.5 L0,7 z" style={{ fill: 'var(--color-accent)' }} />
    </marker>
  </defs>
  {[7, 3, 1, 5].map((v, i) => (
    <g key={i}>
      <rect x={177 + i * 90} y="84" width="76" height="56" rx="4" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.6" />
      <text x={215 + i * 90} y="120" textAnchor="middle" fontSize="24" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
    </g>
  ))}
  <text x="100" y="72" textAnchor="middle" fontSize="14" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>pushFront</text>
  <line x1="46" y1="100" x2="170" y2="100" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#deque-punta)" />
  <line x1="170" y1="126" x2="46" y2="126" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#deque-punta)" />
  <text x="100" y="152" textAnchor="middle" fontSize="14" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>popFront</text>
  <text x="600" y="72" textAnchor="middle" fontSize="14" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>pushBack</text>
  <line x1="654" y1="100" x2="530" y2="100" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#deque-punta)" />
  <line x1="530" y1="126" x2="654" y2="126" style={{ stroke: 'var(--color-accent)' }} strokeWidth="1.8" markerEnd="url(#deque-punta)" />
  <text x="600" y="152" textAnchor="middle" fontSize="14" fontWeight="700" fontFamily="monospace" style={{ fill: 'var(--color-accent)' }}>popBack</text>
  <text x="215" y="166" textAnchor="middle" fontSize="12" fontWeight="600" style={{ fill: 'var(--color-ink-soft)' }}>el frente</text>
  <text x="485" y="166" textAnchor="middle" fontSize="12" fontWeight="600" style={{ fill: 'var(--color-ink-soft)' }}>el final</text>
  <text x="350" y="190" textAnchor="middle" fontSize="13" style={{ fill: 'var(--color-ink-faint)' }}>ningún acceso por posición</text>
</svg>

Con esas cuatro operaciones, los dos contratos anteriores quedan adentro.
Una pila es un deque del que sólo se usan `pushFront` y `popFront`; una cola
es un deque del que sólo se usan `pushBack` y `popFront`. Lo que un deque
sigue sin ofrecer es acceso por posición: sus dos extremos son visibles, y
nada entre ellos.

La implementación no trae ideas nuevas. Sobre una lista doblemente enlazada
con `head` y `tail`, las cuatro operaciones son constantes, porque cada
extremo está en la mano y cada nodo sabe quién viene antes.

Con una trampa, porque retroceder no es simétrico de avanzar. Avanzar es
`(i + 1) % capacidad`; retroceder **no** es `(i - 1) % capacidad`, porque en
Java el resto de un negativo es negativo y `(0 - 1) % 6` da `-1` y no `5`.
Hay que escribir `(i - 1 + capacidad) % capacidad`.

</Slide>

<Slide title="Deque en el mundo real">
El deque aparece menos como protagonista que como pieza, y conviene saber
dónde.

**`std::deque`, en la biblioteca estándar de C++.** Lo interesante es cómo
está hecho: no es un arreglo dinámico ni una lista enlazada, sino un arreglo
de bloques. Los elementos viven juntos de a bloques y los bloques se
indexan aparte, lo que le permite crecer por los dos extremos sin copiar
todo y conservar buena parte de la localidad del arreglo.

**El deque monótono**, una técnica de programación competitiva. Sobre una
ventana que se desplaza por una secuencia, mantiene un deque cuyos valores
quedan siempre ordenados: lo que entra por atrás expulsa a los que ya no
pueden ser máximo, y lo que sale por adelante es lo que quedó fuera de la
ventana. Sirve para responder el máximo de cada ventana en tiempo constante
amortizado, y es el ejemplo más claro de un algoritmo que necesita las dos
puntas al mismo tiempo.

**Los planificadores con robo de trabajo**, en bibliotecas de paralelismo.
Cada hilo tiene su propio deque de tareas: trabaja en su extremo, y un hilo
que se quedó sin trabajo roba del extremo contrario. Los dos extremos son lo
que hace que el dueño y el ladrón casi nunca se estorben.

Los tres usos son especializados, y cada uno pediría su propia maquinaria
para desarrollarse en serio. Por eso esta clase lo nombra y no lo desarrolla:
lo que hay que llevarse es el contrato —dos puntas abiertas— y la capacidad
de reconocerlo cuando aparezca.

</Slide>

## Ejercicios con solución

Cinco ejercicios para resolver en el navegador, **en orden de dificultad**.
Los tres primeros son de cola: uno que la usa sola y dos que le suman una
pila para dar vuelta un tramo. Los dos últimos son el mismo patrón —la
**pila monótona** de la lámina anterior— visto desde dos ángulos opuestos, y
conviene dejarlos para el final porque son los que piden una idea nueva y no
sólo aplicar un contrato.

Ninguno necesita más estructuras que las de esta clase, y las soluciones
están escritas con arreglos y dos índices, sin bibliotecas. Conviene mirar
la solución después de intentarlo: lo que hay que sacar de ella no es el
código, sino por qué cada línea está donde está.

**Cómo se lee una pila escrita a mano.** Las soluciones no llaman a
`push` y `pop`: los escriben directamente sobre un arreglo con un índice
`top` que marca cuántos hay. La correspondencia es esta, y conviene
tenerla a mano al leer:

| El contrato | Escrito a mano |
|---|---|
| `push(x)` | `stack[top++] = x` |
| `pop()` | `stack[--top]` |
| `top()` | `stack[top - 1]` |
| `empty()` | `top == 0` |

El `++` después del índice escribe y **luego** avanza; el `--` antes
retrocede y **luego** lee. Es la misma pila de la clase, sin la clase.

Para la cola, igual con dos índices: `queue[rear++] = x` encola y
`queue[front++]` desencola. Ahí `front` avanza en vez de correr a los demás,
que es lo que el arreglo de la clase no hacía — pero para eso el bloque
tiene que ser lo bastante grande, y cada solución dice cuánto necesita.

<Slide title="La pila monótona">
Hay una técnica que dos de los ejercicios de esta clase necesitan y que no
sale de ninguna lámina anterior. Vale la pena verla una vez, aquí.

El problema: para cada elemento de un arreglo, encontrar el primero a su
derecha que sea mayor. La solución ingenua mira, por cada posición, todo lo
que sigue: $$\Theta(N^2)$$.

La observación que la arregla es que **cuando llega un elemento grande,
resuelve de una vez a todos los que estaban esperando y son menores que
él**. Si los que esperan se guardan en una pila, quedan en orden no
creciente, así que alcanza con sacar desde la cima hasta encontrar uno
mayor. Con `[4, 5, 2, 25]`:

| llega | resuelve | la pila queda |
|---|---|---|
| `4` | a nadie | `4` |
| `5` | al `4` → su mayor es 5 | `5` |
| `2` | a nadie | `5 2` |
| `25` | al `2` y al `5` → el mayor de ambos es 25 | `25` |
| fin | el `25` no fue superado por nadie → `-1` | — |

Lo que hay que guardar son **posiciones** y no valores: al resolver una hay
que saber en qué casilla del resultado escribir.

Y el costo, que es la parte que sorprende: hay un ciclo dentro de otro y aun
así es $$\Theta(N)$$. **Cada posición entra a la pila exactamente una vez y
sale a lo más una vez**, así que el ciclo de adentro no multiplica el
trabajo — lo reparte. Sumadas todas sus vueltas, son $$N$$ en total.

</Slide>

<Exercise title="Los binarios de 1 a N">

Escribe `binaries`, que devuelve la representación binaria de los números de
1 a `n`, en orden, como un arreglo de `String`.

Se puede resolver convirtiendo cada número por separado, pero hay una
solución más corta y es de cola. Cada binario produce dos: el mismo texto
con un `0` y con un `1` al final. De `"1"` salen `"10"` y `"11"`; de `"10"`
salen `"100"` y `"101"`. Y como la cola entrega en el orden en que se
encoló, los va devolviendo exactamente en el orden en que hay que
escribirlos.

Encola `"1"`. Después, `n` veces: saca uno del frente, guárdalo en el
resultado, y encola sus dos hijos —el mismo texto con un `0` y con un `1` al
final.

```java starter
class Solution {
    static String[] binaries(int n) {
        return new String[n];
    }
}
```

```java test
String[] r = Solution.binaries(5);
check(r.length, 5);
check(r[0], "1");
check(r[1], "10");
check(r[2], "11");
check(r[3], "100");
check(r[4], "101");
check(Solution.binaries(1).length, 1);
check(Solution.binaries(0).length, 0);
```

```java solution
class Solution {
    // La cola es un arreglo con dos índices: `front` por donde sale,
    // `rear` por donde entra. Aquí `rear` es la próxima casilla LIBRE —se
    // escribe y después se avanza—. Aquí `front` avanza en vez de correr
    // a los demás. Se sacan n y se encolan 1 + 2n, así que
    // 2n + 2 casillas alcanzan y sobran.
    static String[] binaries(int n) {
        String[] result = new String[n];
        String[] queue = new String[2 * n + 2];
        int front = 0;
        int rear = 0;
        queue[rear++] = "1";
        for (int i = 0; i < n; i++) {
            String s = queue[front++];
            result[i] = s;
            queue[rear++] = s + "0";
            queue[rear++] = s + "1";
        }
        return result;
    }
}
```

</Exercise>

<Exercise title="Invertir los primeros K de una cola">

Escribe `reverseFirstK`, que recibe una cola —dada como arreglo, con el
frente en la posición 0— y devuelve la cola resultante de invertir sus
primeros `k` elementos, dejando el resto en su orden. Con `[1, 2, 3, 4, 5]`
y `k = 3` el resultado es `[3, 2, 1, 4, 5]`.

La cola sola no puede: sacar por el frente y meter por el final nunca
invierte nada. Lo que falta lo aporta la pila, que es exactamente la
estructura que devuelve las cosas al revés.

Son tres fases, y la tercera es la que sorprende. Los primeros `k` salen de
la cola y entran a la pila; vuelven a la cola desde la pila, y como entran
por el final quedan invertidos **pero atrás**; entonces hay que rotar los
`n - k` que no se tocaron —sacarlos del frente y volver a encolarlos— para
que el tramo invertido vuelva a quedar adelante.

Con `[1, 2, 3, 4, 5]` y `k = 3`, fase por fase:

<StepShow
  language="java"
  code={`for (int i = 0; i < k; i++) {
    stack[top++] = queue[front++];
}
while (top > 0) {
    queue[rear++] = stack[--top];
}
for (int i = 0; i < n - k; i++) {
    queue[rear++] = queue[front++];
}`}
>
  <Step lines={[]}>
    <svg viewBox="0 0 560 128" style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }} role="img" aria-label="Fase inicial: la pila está vacía y la cola contiene 1, 2, 3, 4, 5.">
      <text x="8" y="36" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>pila</text>
      <text x="70" y="36" fontSize="14" style={{ fill: 'var(--color-ink-faint)' }}>vacía</text>
      <text x="8" y="94" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>cola</text>
      {[1, 2, 3, 4, 5].map((v, i) => (
        <g key={i}>
          <rect x={70 + i * 62} y="70" width="54" height="34" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
          <text x={97 + i * 62} y="93" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
        </g>
      ))}
      <text x="70" y="122" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>la cola tal como llega · el frente es el de la izquierda</text>
    </svg>
  </Step>
  <Step lines={[1, 2, 3]}>
    <svg viewBox="0 0 560 128" style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }} role="img" aria-label="La pila contiene 1, 2, 3 con el 3 en la cima; la cola contiene 4 y 5.">
      <text x="8" y="36" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>pila</text>
      {[1, 2, 3].map((v, i) => (
        <g key={i}>
          <rect x={70 + i * 62} y="14" width="54" height="34" rx="3" style={{ fill: 'var(--color-mark-soft)', stroke: 'var(--color-mark)' }} strokeWidth="1.5" />
          <text x={97 + i * 62} y="37" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
        </g>
      ))}
      <text x="262" y="37" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>← cima</text>
      <text x="8" y="94" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>cola</text>
      {[4, 5].map((v, i) => (
        <g key={i}>
          <rect x={70 + i * 62} y="70" width="54" height="34" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" />
          <text x={97 + i * 62} y="93" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
        </g>
      ))}
      <text x="200" y="93" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>salen del frente en orden, así que la pila los tiene al revés</text>
    </svg>
  </Step>
  <Step lines={[4, 5, 6]}>
    <svg viewBox="0 0 560 128" style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }} role="img" aria-label="La pila quedó vacía; la cola contiene 4, 5, 3, 2, 1: el tramo invertido quedó al final.">
      <text x="8" y="36" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>pila</text>
      <text x="70" y="36" fontSize="14" style={{ fill: 'var(--color-ink-faint)' }}>vacía</text>
      <text x="8" y="94" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>cola</text>
      {[4, 5, 3, 2, 1].map((v, i) => (
        <g key={i}>
          <rect x={70 + i * 62} y="70" width="54" height="34" rx="3" style={{ fill: i >= 2 ? 'var(--color-mark-soft)' : 'var(--color-surface)', stroke: i >= 2 ? 'var(--color-mark)' : 'var(--color-rule-strong)' }} strokeWidth="1.5" />
          <text x={97 + i * 62} y="93" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
        </g>
      ))}
      <text x="70" y="122" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>invertidos, pero atrás — éste es el paso que no basta</text>
    </svg>
  </Step>
  <Step lines={[7, 8, 9]}>
    <svg viewBox="0 0 560 128" style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }} role="img" aria-label="Resultado: la cola contiene 3, 2, 1, 4, 5, con el tramo invertido adelante.">
      <text x="8" y="36" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>pila</text>
      <text x="70" y="36" fontSize="14" style={{ fill: 'var(--color-ink-faint)' }}>vacía</text>
      <text x="8" y="94" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>cola</text>
      {[3, 2, 1, 4, 5].map((v, i) => (
        <g key={i}>
          <rect x={70 + i * 62} y="70" width="54" height="34" rx="3" style={{ fill: i < 3 ? 'var(--color-mark-soft)' : 'var(--color-surface)', stroke: i < 3 ? 'var(--color-mark)' : 'var(--color-rule-strong)' }} strokeWidth="1.5" />
          <text x={97 + i * 62} y="93" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text>
        </g>
      ))}
      <text x="70" y="122" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>el 4 y el 5 dieron una vuelta y el tramo invertido quedó adelante</text>
    </svg>
  </Step>
</StepShow>

```java starter
class Solution {
    static int[] reverseFirstK(int[] input, int k) {
        return input;
    }
}
```

```java test
check(Solution.reverseFirstK(new int[]{1, 2, 3, 4, 5}, 3), new int[]{3, 2, 1, 4, 5});
check(Solution.reverseFirstK(new int[]{1, 2, 3, 4, 5}, 5), new int[]{5, 4, 3, 2, 1});
check(Solution.reverseFirstK(new int[]{1, 2, 3, 4, 5}, 0), new int[]{1, 2, 3, 4, 5});
check(Solution.reverseFirstK(new int[]{7}, 1), new int[]{7});
```

```java solution
class Solution {
    // La cola vive en un arreglo con `front` y `rear`, sin dar la vuelta: se
    // encolan n + k + (n - k) = 2n elementos en total, así que 2n casillas
    // alcanzan y no hace falta el módulo.
    static int[] reverseFirstK(int[] input, int k) {
        int n = input.length;
        int[] queue = new int[2 * n];
        int front = 0;
        int rear = 0;
        for (int x : input) {
            queue[rear++] = x;
        }

        int[] stack = new int[k];
        int top = 0;
        for (int i = 0; i < k; i++) {       // los primeros k salen de la cola
            stack[top++] = queue[front++];
        }
        while (top > 0) {                   // y vuelven al final, invertidos
            queue[rear++] = stack[--top];
        }
        for (int i = 0; i < n - k; i++) {   // los intactos rotan una vuelta
            queue[rear++] = queue[front++];
        }

        int[] result = new int[n];
        for (int i = 0; i < n; i++) {
            result[i] = queue[front++];
        }
        return result;
    }
}
```

</Exercise>

<Exercise title="Intercalar las dos mitades de una cola">

Escribe `interleave`, que recibe una cola con una cantidad **par** de
elementos y devuelve la cola con las dos mitades intercaladas: el primero de
la primera mitad, el primero de la segunda, el segundo de la primera, y así.
Con `[1, 2, 3, 4, 5, 6]` el resultado es `[1, 4, 2, 5, 3, 6]`.

Es el ejercicio más largo de los cinco y el que mejor muestra para qué sirve
tener las dos estructuras a mano. La primera mitad hay que poder
adelantarla; la segunda hay que poder dejarla quieta. Ninguna de las dos
cosas la hace un solo contrato.

El plan tiene cuatro movimientos. La primera mitad va a la pila y vuelve a
la cola, con lo que queda invertida al final; una rotación de media vuelta
la devuelve adelante, ahora al derecho. Después la primera mitad vuelve a la
pila, y esta vez se intercala: por cada elemento que sale de la pila, uno
sale del frente de la cola y vuelve al final.

Con `[1, 2, 3, 4, 5, 6]`, los cinco estados por los que pasa la cola:

<StepShow
  language="java"
  code={`for (int i = 0; i < half; i++) stack[top++] = queue[front++];
while (top > 0) queue[rear++] = stack[--top];
for (int i = 0; i < half; i++) queue[rear++] = queue[front++];
for (int i = 0; i < half; i++) stack[top++] = queue[front++];
while (top > 0) {
    queue[rear++] = stack[--top];
    queue[rear++] = queue[front++];
}`}
>
  <Step lines={[1]}>
    <svg viewBox="0 0 560 128" style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }} role="img" aria-label="La pila tiene 1, 2, 3 con el 3 en la cima; la cola tiene 4, 5, 6.">
      <text x="8" y="36" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>pila</text>
      {[1, 2, 3].map((v, i) => (<g key={i}><rect x={70 + i * 62} y="14" width="54" height="34" rx="3" style={{ fill: 'var(--color-mark-soft)', stroke: 'var(--color-mark)' }} strokeWidth="1.5" /><text x={97 + i * 62} y="37" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text></g>))}
      <text x="262" y="37" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>← cima</text>
      <text x="8" y="94" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>cola</text>
      {[4, 5, 6].map((v, i) => (<g key={i}><rect x={70 + i * 62} y="70" width="54" height="34" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" /><text x={97 + i * 62} y="93" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text></g>))}
      <text x="70" y="122" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>la primera mitad sale del frente y queda en la pila, al revés</text>
    </svg>
  </Step>
  <Step lines={[2]}>
    <svg viewBox="0 0 560 128" style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }} role="img" aria-label="La pila quedó vacía; la cola tiene 4, 5, 6, 3, 2, 1.">
      <text x="8" y="36" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>pila</text>
      <text x="70" y="36" fontSize="14" style={{ fill: 'var(--color-ink-faint)' }}>vacía</text>
      <text x="8" y="94" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>cola</text>
      {[4, 5, 6, 3, 2, 1].map((v, i) => (<g key={i}><rect x={70 + i * 62} y="70" width="54" height="34" rx="3" style={{ fill: i >= 3 ? 'var(--color-mark-soft)' : 'var(--color-surface)', stroke: i >= 3 ? 'var(--color-mark)' : 'var(--color-rule-strong)' }} strokeWidth="1.5" /><text x={97 + i * 62} y="93" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text></g>))}
      <text x="70" y="122" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>vuelve a la cola por el final: invertida, pero atrás</text>
    </svg>
  </Step>
  <Step lines={[3]}>
    <svg viewBox="0 0 560 128" style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }} role="img" aria-label="La cola tiene 3, 2, 1, 4, 5, 6: el tramo invertido quedó adelante.">
      <text x="8" y="36" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>pila</text>
      <text x="70" y="36" fontSize="14" style={{ fill: 'var(--color-ink-faint)' }}>vacía</text>
      <text x="8" y="94" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>cola</text>
      {[3, 2, 1, 4, 5, 6].map((v, i) => (<g key={i}><rect x={70 + i * 62} y="70" width="54" height="34" rx="3" style={{ fill: i < 3 ? 'var(--color-mark-soft)' : 'var(--color-surface)', stroke: i < 3 ? 'var(--color-mark)' : 'var(--color-rule-strong)' }} strokeWidth="1.5" /><text x={97 + i * 62} y="93" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text></g>))}
      <text x="70" y="122" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>media vuelta, y el tramo invertido queda adelante al derecho</text>
    </svg>
  </Step>
  <Step lines={[4]}>
    <svg viewBox="0 0 560 128" style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }} role="img" aria-label="La pila tiene 3, 2, 1 con el 1 en la cima; la cola tiene 4, 5, 6.">
      <text x="8" y="36" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>pila</text>
      {[3, 2, 1].map((v, i) => (<g key={i}><rect x={70 + i * 62} y="14" width="54" height="34" rx="3" style={{ fill: 'var(--color-mark-soft)', stroke: 'var(--color-mark)' }} strokeWidth="1.5" /><text x={97 + i * 62} y="37" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text></g>))}
      <text x="262" y="37" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>← cima</text>
      <text x="8" y="94" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>cola</text>
      {[4, 5, 6].map((v, i) => (<g key={i}><rect x={70 + i * 62} y="70" width="54" height="34" rx="3" style={{ fill: 'var(--color-surface)', stroke: 'var(--color-rule-strong)' }} strokeWidth="1.5" /><text x={97 + i * 62} y="93" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text></g>))}
      <text x="70" y="122" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>vuelve a la pila, y ahora el 1 es el que queda en la cima</text>
    </svg>
  </Step>
  <Step lines={[5, 6, 7, 8]}>
    <svg viewBox="0 0 560 128" style={{ width: '100%', maxWidth: '560px', height: 'auto', display: 'block' }} role="img" aria-label="Resultado: la cola tiene 1, 4, 2, 5, 3, 6, con las dos mitades intercaladas.">
      <text x="8" y="36" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>pila</text>
      <text x="70" y="36" fontSize="14" style={{ fill: 'var(--color-ink-faint)' }}>vacía</text>
      <text x="8" y="94" fontSize="13" fontFamily="monospace" style={{ fill: 'var(--color-ink-faint)' }}>cola</text>
      {[1, 4, 2, 5, 3, 6].map((v, i) => (<g key={i}><rect x={70 + i * 62} y="70" width="54" height="34" rx="3" style={{ fill: i % 2 === 0 ? 'var(--color-mark-soft)' : 'var(--color-surface)', stroke: i % 2 === 0 ? 'var(--color-mark)' : 'var(--color-rule-strong)' }} strokeWidth="1.5" /><text x={97 + i * 62} y="93" textAnchor="middle" fontSize="16" fontWeight="600" style={{ fill: 'var(--color-ink)' }}>{v}</text></g>))}
      <text x="70" y="122" fontSize="12" style={{ fill: 'var(--color-ink-faint)' }}>uno de la pila, uno de la cola · las de la primera mitad, marcadas</text>
    </svg>
  </Step>
</StepShow>

```java starter
class Solution {
    static int[] interleave(int[] input) {
        return input;
    }
}
```

```java test
check(Solution.interleave(new int[]{1, 2, 3, 4, 5, 6}), new int[]{1, 4, 2, 5, 3, 6});
check(Solution.interleave(new int[]{1, 2, 3, 4}), new int[]{1, 3, 2, 4});
check(Solution.interleave(new int[]{9, 8}), new int[]{9, 8});
check(Solution.interleave(new int[]{1, 2, 3, 4, 5, 6, 7, 8}), new int[]{1, 5, 2, 6, 3, 7, 4, 8});
```

```java solution
class Solution {
    // Cuántas casillas hacen falta: se encolan n al principio, mitad al
    // volver de la pila, mitad al rotar y 2 × mitad al intercalar. Son
    // n + 4 × (n/2) = 3n encoladas en total, así que 3n + 1 alcanza.
    static int[] interleave(int[] input) {
        int n = input.length;
        int half = n / 2;
        int[] queue = new int[3 * n + 1];
        int front = 0;
        int rear = 0;
        for (int x : input) {
            queue[rear++] = x;
        }

        int[] stack = new int[half];
        int top = 0;
        for (int i = 0; i < half; i++) {   // la primera mitad a la pila
            stack[top++] = queue[front++];
        }
        while (top > 0) {                   // vuelve al final, invertida
            queue[rear++] = stack[--top];
        }
        for (int i = 0; i < half; i++) {   // media vuelta: queda adelante
            queue[rear++] = queue[front++];
        }

        for (int i = 0; i < half; i++) {   // otra vez a la pila
            stack[top++] = queue[front++];
        }
        while (top > 0) {                   // y se intercalan de a uno
            queue[rear++] = stack[--top];
            queue[rear++] = queue[front++];
        }

        int[] result = new int[n];
        for (int i = 0; i < n; i++) {
            result[i] = queue[front++];
        }
        return result;
    }
}
```

</Exercise>

<Exercise title="Next Greater Element">

Escribe `nextGreater`, que recibe un arreglo y devuelve otro del mismo
largo: en cada posición, el primer elemento **estrictamente mayor** que
aparece a su derecha, o `-1` si no hay ninguno.

La solución ingenua mira, para cada posición, todo lo que sigue: $$\Theta(N^2)$$.
La pila la reduce a $$\Theta(N)$$ con una observación. Recorriendo de
izquierda a derecha, cuando llega un elemento grande, **resuelve de una vez
a todos los que estaban esperando y son menores que él** — y los que
esperan, si se guardan en una pila, quedan en orden no creciente, así que
alcanza con sacar desde la cima hasta encontrar uno mayor.

Guarda en la pila **posiciones**, no valores: al resolver una hay que saber
en qué casilla del resultado escribir. Las que quedan en la pila al terminar
son las que nadie superó.

```java starter
class Solution {
    static int[] nextGreater(int[] a) {
        return new int[a.length];
    }
}
```

```java test
check(Solution.nextGreater(new int[]{4, 5, 2, 25}), new int[]{5, 25, 25, -1});
check(Solution.nextGreater(new int[]{13, 7, 6, 12}), new int[]{-1, 12, 12, -1});
check(Solution.nextGreater(new int[]{3, 3}), new int[]{-1, -1});
check(Solution.nextGreater(new int[]{}), new int[]{});
```

```java solution
class Solution {
    // La pila guarda POSICIONES cuyos valores quedan en orden no creciente:
    // la comparación es estricta, así que dos iguales conviven — y por eso
    // [3, 3] responde -1 dos veces.
    // Cada posición entra una vez y sale una vez: el ciclo de adentro no
    // multiplica el costo, lo reparte.
    static int[] nextGreater(int[] a) {
        int[] result = new int[a.length];
        int[] stack = new int[a.length];
        int top = 0;
        for (int i = 0; i < a.length; i++) {
            while (top > 0 && a[stack[top - 1]] < a[i]) {
                result[stack[--top]] = a[i];   // a[i] es el primer mayor del que esperaba
            }
            stack[top++] = i;
        }
        while (top > 0) {
            result[stack[--top]] = -1;         // nadie los superó
        }
        return result;
    }
}
```

</Exercise>

<Exercise title="Stock Span">

Escribe `span`, que recibe los precios de una acción día a día y devuelve,
para cada día, cuántos días consecutivos hacia atrás —contando el día
mismo— el precio fue **menor o igual** que el de hoy.

Es el problema anterior mirado al revés: en vez de preguntar quién me supera
a la derecha, pregunta hasta dónde llego hacia la izquierda sin encontrar a
nadie mayor. Y hay un segundo cambio que el enunciado esconde: aquí la
comparación **deja de ser estricta**. El enunciado dice «menor o igual», así
que los días con el mismo precio sí cuentan para el span — por eso el
`while` usa `<=` donde el ejercicio anterior usaba `<`. La pila vuelve a guardar posiciones, y el precio de hoy expulsa
a todas las que no lo superan —ninguna de ellas podrá volver a cortar a
nadie, porque hoy está más a la derecha y es mayor.

Cuando la pila queda vacía, hoy es el máximo de todo lo visto y el span son
los `i + 1` días. Si no, el que quedó en la cima es el primer día anterior
con precio mayor, y ahí se corta.

```java starter
class Solution {
    static int[] span(int[] prices) {
        return new int[prices.length];
    }
}
```

```java test
check(Solution.span(new int[]{100, 80, 60, 70, 60, 75, 85}), new int[]{1, 1, 1, 2, 1, 4, 6});
check(Solution.span(new int[]{10, 4, 5, 90, 120, 80}), new int[]{1, 1, 2, 4, 5, 1});
check(Solution.span(new int[]{5}), new int[]{1});
check(Solution.span(new int[]{}), new int[]{});
```

```java solution
class Solution {
    // La misma pila monótona de posiciones, con la comparación dada vuelta:
    // hoy expulsa a todos los días anteriores que no lo superan.
    static int[] span(int[] prices) {
        int[] result = new int[prices.length];
        int[] stack = new int[prices.length];
        int top = 0;
        for (int i = 0; i < prices.length; i++) {
            while (top > 0 && prices[stack[top - 1]] <= prices[i]) {
                top--;
            }
            // pila vacía: hoy es el máximo hasta aquí. Si no, la cima es el
            // primer día anterior con precio mayor, y el span llega hasta él.
            result[i] = (top == 0) ? i + 1 : i - stack[top - 1];
            stack[top++] = i;
        }
        return result;
    }
}
```

</Exercise>

<Slide title="El precio de decidir por orden de llegada">
Los dos contratos de esta clase deciden quién sale con el mismo criterio:
**cuándo entró**. La pila saca al último, la cola saca al primero, y ninguno
de los dos mira lo que el elemento trae adentro.

Esa es la regla que compra los costos constantes. El elemento que sale está
siempre en un extremo conocido, así que no hay que buscarlo. Y es también la
única cosa que estos contratos no pueden hacer: si lo que hay que sacar es
**el más urgente**, ningún extremo lo tiene garantizado, y encontrarlo
obligaría a mirar los $$N$$ elementos — que es justamente lo que el contrato
no permite.

**¿Y si renunciamos al orden de llegada?**

Si el que sale es el de mayor prioridad, la colección deja de poder prometer
que el elemento que hay que sacar está en un extremo, y entonces tiene que
sostener algo más fuerte: un orden parcial entre sus elementos, mantenido en
cada inserción.

Ese contrato existe y se llama **cola con prioridad**.

El intercambio conviene contarlo desde donde estamos, no desde donde vamos.
Hoy, con una pila o una cola, sacar al más urgente cuesta $$\Theta(N)$$:
hay que mirarlos todos. Una implementación buena de la cola con prioridad lo
deja en $$\Theta(\log N)$$, y cobra $$\Theta(\log N)$$ por insertar, que
hasta ahora era gratis. Se paga en la inserción lo que se gana en la
extracción — y se gana mucho más de lo que se paga en cuanto las
extracciones importan.

Los costos, eso sí, no son del contrato: un TDA sigue sin tenerlos. Son de
la estructura que lo cumple, y ésa es la clase siguiente.

</Slide>

<Slide title="Lo que sigue">
La próxima clase estudia la **cola con prioridad**: el mismo contrato de una
cola —se encola, se desencola, se consulta el frente— con una sola regla
cambiada. El frente no es el que llegó primero, es el de mayor prioridad.

El cambio se nota apenas se mira dónde importa. En una urgencia médica la
atención no se reparte por orden de llegada sino por gravedad. En un sistema
operativo, entre todos los procesos que esperan turno de procesador, el
planificador elige por prioridad y no por antigüedad. En los dos casos el
contrato de esta clase se queda corto, y por la misma razón.

La estructura que lo cumple bien es el **montículo**, un árbol guardado
dentro de un arreglo, donde cada elemento es mayor o igual que sus hijos. Es la
primera estructura del curso que no es una secuencia, y con ella empieza la
parte de la unidad donde la forma deja de ser una fila.

</Slide>
